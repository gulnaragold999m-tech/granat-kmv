# STATUS.md — что происходит сейчас

Обновлено: 16 августа 2026, после восстановления сайта.

## Живой сайт `granat-kmv.ru` — работает

Приложение Amvera `granat-site`, Python + Flask, запуск через gunicorn.

Ночью с 15 на 16 августа сайт лежал: загрузили новый `app.py` без папки
`templates`, и каждому посетителю уходила ошибка. В 10:50 16.08 папку
вернули, сайт открылся. Разбор — в `TASKS.md`.

Проверено 16.08.2026 по `granat-kmv.ru/api/health`:

- `vk: ok` — заявка уходит во ВКонтакте.
- `mail: ok` — и на почту `gulnaravibecoder999@yandex.ru`.
- `undelivered: 0` — застрявших заявок нет.
- `telegram: снят с сайта 15.08.2026` — так и задумано.

## Каналы заявок: ВКонтакте и почта

Telegram убран 15.08.2026 решением владелицы, её словами: «Телеграм как
трафик для заявок снимаем. У меня телеграм не работает — я вижу, когда
приходит информация, но открыть не могу».

Канал, до которого нельзя дотянуться, хуже отсутствующего: заявка
числится доставленной, счётчик молчит, а прочитать её нельзя. Поэтому
убран целиком, а не отключён на время. Место для возврата помечено
в коде.

Заявка пишется на диск `/data` **до** всякой отправки. Молчат оба канала —
она всё равно не потеряна.

## Джарвис — бот в Telegram и ВКонтакте

Отдельное приложение Amvera `jarvis-bot`. Telegram-транспорт —
`jarvis_bot.py`, ВКонтакте — `vk_jarvis.py`, движок диалога общий.

**Не проверено в бою.** Живого клиента через ВК-версию ещё не проводили.
Проверка: написать в сообщество с личной страницы и пройти путь до конца;
в логах `jarvis-bot` при старте должна быть строка «🔵 ВКонтакте: слушает
сообщения сообщества».

Это важно потому, что сайт теперь отправляет клиентов именно во
ВКонтакте. Если бот там молчит, каждого пришедшего ждёт Гульнара лично.

## Сайт на Next.js в корне репозитория — не опубликован

Живого адреса у него нет. Собирается, страницы `/pechat`, `/cifra`,
`/kontakty` и главная на месте, прайс сведён в `data/prices.ts`. К нему
можно вернуться, если решат менять живой сайт на этот; пока он лежит.

## Чем это всё держится — и где тонко

1. **Обе программы существуют в одном экземпляре**, в Amvera. В
   репозитории копии, разной свежести. `speech.py` не существует нигде,
   кроме Amvera.
2. **Проверки, которая ловит вчерашнюю поломку, нет.** `/api/health`
   отвечал `ok: true`, пока сайт отдавал ошибку каждому посетителю.
3. **Сторожа снаружи нет.** О простое узнали от владелицы через ночь.

Все три записаны хвостами в `TASKS.md` за 16.08.2026.
