# TASKS.md — что делать

Порядок сверху вниз. Отмечать `[x]` по мере готовности и коротко писать, что
именно сделано.

## Сейчас в работе

- [ ] **Довести заявку до владелицы.** Переписать `app/api/lead/route.ts` так,
      чтобы заявка уходила в Telegram и/или на почту. Пока не сделано — форма
      молча теряет обращения. Ждём ответа: Telegram, почта или оба.

## Срочное

- [ ] **Выяснить, откуда Amvera берёт код сайта.** Репозиторий и живой сайт
      расходятся: на granat-kmv.ru есть первый экран, которого нет в коде.
      Пока не выяснено — непонятно, доедет ли до сайта хоть одна правка.
- [ ] **Вернуть главную страницу в сборку.** Перенести `page.tsx`, `layout.tsx`
      и `globals.css` из корня в папку `app/`. Сейчас Next.js их не видит:
      нет главной, нет меню и футера, не подключён Tailwind.
- [ ] **Сделать страницы `/privacy` и `/consent`.** На них ссылается галочка
      согласия в форме заявки. Собираем персональные данные — по 152-ФЗ эти
      страницы обязаны существовать, сейчас обе дают 404.

## Важное

- [ ] Перенести `robots.txt`, `sitemap.xml`, `favicon.ico` и `*.svg` из корня
      в папку `public/` — иначе поисковики не видят robots и карту сайта.
- [ ] Сделать страницы `/portfolio` и `/o-nas`. Ссылки на них уже стоят в меню
      и в карте сайта, обе ведут в 404.
- [ ] Развести первый экран: печать отдельно, веб отдельно. Человеку, которому
      нужны наклейки или баннер, сейчас приходится продираться через текст
      про сайты и код.
- [ ] Свести почту к одному адресу: в футере и README стоит
      `gulnaravibecoder999@yandex.ru`, на странице контактов — `info@granat-kmv.ru`.

## Мелочи

- [ ] Опечатка «корпративы» → «корпоративы», `app/pechat/page.tsx`.
- [ ] На странице контактов адрес указан как «Лермонтово». Город называется
      Лермонтов.
- [ ] `.env.example` описывает `DATABASE_URL` и `API_URL`, которых нет в коде.
      Привести к тому, что реально используется.

## Сделано

- [x] Проверена форма заявки. Подозрение подтвердилось: `route.ts` пишет заявку
      в консоль сервера и отвечает «успешно», не отправляя никуда.
- [x] Заведены `CLAUDE.md`, `STATUS.md`, `TASKS.md`.
