# Журнал работ и незакрытые хвосты — granat-kmv

Свежее сверху. Сделал — вычеркнул и коротко записал, что именно вышло.

Здесь сведены три журнала, которые до 16.08.2026 жили в разных ветках:
бот и прайс (`granat-kmv-multipage-site-6h04uz`), сайт на Next.js
(`sozdala-fppb9e`) и живой сайт (`connect-marketer-yfwc8q`). Новых
журналов не заводить.

## 16.08.2026 — в прайс добавлен формат А4 для сертификатов

Пришёл заказ: сертификат христианского лагеря, горизонтальный А4,
полноцвет, один экземпляр. Посчитали по прайсу — вышло **50 ₽**
(цветная печать А4 стоит 35 ₽, минимальный заказ на распечатку 50 ₽).
Реакция владелицы: «что я заработаю на разовых заказах 1 шт 50 рублей».

Она права, и дело было не в клиенте, а в строке прайса. Разовый заказ —
это десять минут с человеком, бумага и тонер съедают около десятки.
Остаётся 40 ₽ за десять минут, то есть 240 ₽ в час.

**Что нашли.** Сертификата в прайсе не было вовсе — он стоял в списке
`NET_V_PRAJSE` как услуга без цены. При этом граница в прайсе уже была
проведена: «бумага плотнее 250 г/м² считается по разделу "Открытки и
приглашения"». Сертификат на картоне — не распечатка, а изделие, и
минимальный заказ по этому разделу 700 ₽, а не 50 ₽. Но формата **А4
в разделе открыток не существовало** — только А6, Евро и А5. Цену для
А4 приходилось брать с потолка.

**Что сделано** (`data/prices.ts`):

- В таблицу «Открытки, поздравления» добавлена строка **А4:
  150 / 120 / 95 / 80 ₽** за штуку при тиражах 10–20 / 50 / 100 / 200.
  Цифры согласованы с владелицей 16.08.2026. Внутри таблицы шаг между
  форматами примерно ×1,5 (А6 → А5: 60 → 90); для А4 взято ×1,7 — лист
  картона уходит целиком и брак дороже.
- Примечанием закреплено: сертификаты, дипломы и грамоты на картоне
  считаются по этой таблице, а не как распечатка А4.
- **`сертификаты` убраны из `NET_V_PRAJSE`** — цена появилась, значит
  услугу можно называть и продавать.
- Проверено `node --experimental-strip-types scripts/ceny.mts`: строка
  печатается, список «чего нет» сократился.

**Найдено попутно и НЕ исправлено — расхождение живого сайта с прайсом.**
Живой сайт на Flask цены из `data/prices.ts` не берёт: они вписаны
руками в `zhivoj-sajt/templates/partials/ceny.html`. И там сейчас:

- [ ] «Сертификаты — **от 300 ₽**». Ни с чем не совпадает: по новой
      строке это 150 ₽/шт при тираже, а один экземпляр — 700 ₽ по
      минималке раздела. Цену на сайте надо привести к прайсу.
- [ ] «Плоттерная резка — от 300 ₽», хотя плоттерная резка стоит
      в `NET_V_PRAJSE` как услуга, которой у студии нет.
- [ ] Заголовок и описание `zhivoj-sajt/templates/pechat.html` обещают
      визитки и тиснение золотом — обе позиции тоже в списке «чего нет».
      Это видит поиск и видит клиент.

Это ровно та болезнь, от которой заводился единый прайс: **цена,
записанная руками во второй раз, разъезжается.** Живой сайт — второе
место, и он уже разъехался. Чинится либо правкой шаблона по прайсу
(быстро, но разойдётся снова), либо тем, что Flask начнёт брать цены
из одного источника (правильно, но это отдельная работа).

### Поправка через час: цифры взяты у бота, а не сочинены

Владелица велела указать всё — штучный заказ, срочность — и завести это
в бота и на сайт. Полез в бота и выяснил, что **править надо было
не бота, а всё остальное.**

В `prices.py` (ветка `claude/granat-kmv-multipage-site-6h04uz`, работает
с 11.08.2026) уже стояло:

- открытка А4 по готовому макету — **200 ₽/шт при 1–9, 180 ₽ от десяти**;
- `MIN_ORDER[("invites", "A4")]` = **1200 ₽** с пояснением «ниже этой
  суммы работа не окупает приёмку, резку и ручную доработку»;
- `COEFFICIENTS["urgent"]` = **×1,30 на весь итог**, одна ставка
  срочности на весь прайс.

То есть **бот уже месяц считал по-человечески**, а печатный прайс и
живой сайт — по 35 ₽ за лист. На вопрос «сертификат, одна штука,
срочно» бот ответил бы 1560 ₽ (1200 × 1,3), а расчёт по бумажному
прайсу дал 50 ₽. Разница в тридцать раз, и всё это время два канала
называли разные цены.

Поэтому шкала 150/120/95/80, вписанная часом раньше, **заменена на
ботовскую**: 200 ₽ штучно, 180 ₽ от десяти, минимальный заказ 1200 ₽,
срочность +30%. Она была не хуже — она была третьей ценой на ту же
услугу, а это и есть та самая болезнь.

Что сделано:

- `data/prices.ts` — строка А4 приведена к боту, добавлены примечания
  про штучную цену, минимальный заказ и срочность; в «Как мы считаем»
  добавлена строка «штучный сертификат или открытка А4 — от 1200 ₽».
- В файле записано правило: меняете А4 здесь — меняйте `prices.py`
  тем же числом.
- `zhivoj-sajt/templates/partials/ceny.html` — «Сертификаты от 300 ₽»
  заменено на «Сертификаты, дипломы А4 — от 180 ₽/шт», в пояснении под
  списком указаны картон, минимальный заказ 1200 ₽ и срочность +30%.

- [ ] **Выкатить живой сайт.** Правка в репозитории сама на
      `granat-kmv.ru` не попадает: нужно загрузить папку `templates`
      целиком в приложение `granat-site` (папку целиком — 15.08.2026
      на этом сайт лежал ночь).
- [ ] **Скидка за тираж на А4 — решить.** Сейчас от десяти штук цена
      не снижается вовсе. Для заказа на 50 сертификатов это дорого, и
      клиент уйдёт. Решим цифру — править надо В ДВУХ местах:
      `data/prices.ts` и `prices.py` бота.
- [ ] **`prices.py` живёт в чужой ветке и в Amvera.** Пока он не лежит
      в `main` рядом с `data/prices.ts`, их расхождение будет замечать
      человек, а не проверка.

### Шпаргалка у стойки и заказ без параметров

**Шпаргалка.** Прайс — десять страниц, при клиенте их не листают, и цену
называют на глаз. `scripts/shpargalka.mts` собирает `shpargalka.html`:
одна страница А4, две колонки, всё из `data/prices.ts`. Запуск —
`npm run shpargalka`, печатать с включённой фоновой графикой. Сверху
жёлтая рамка «сначала спросить — потом цену», снизу красная «не
обещать: этого у нас нет».

**Пограничный случай с бумагой, разобран в тот же день.** Клиентка
уточнила: фотобумага матовая 230 г/м². Это НЕ раздел открыток — тот
начинается с картона 250 г/м². Считается как печать фотографии А4
(180 ₽) плюс наценка за плотную бумагу 15–25%, итого около 220 ₽ за
штуку, срочно 290 ₽. Записано примечанием в таблицу «Печать
фотографий», чтобы не считать заново.

**Заказ пришёл почтой без единого параметра.** Клиентка прислала на
`gulnaravibecoder999@yandex.ru` файл сертификата — и всё: ни тиража, ни
бумаги, ни срока, ни телефона. Реакция владелицы: «вот что значит заказ
без бота». Бот эти вопросы задаёт сам и дальше не пускает, а почта
принимает что угодно.

На сайте кнопка «Прислать макет почтой» с готовым письмом была и
раньше — но адрес можно скопировать руками, минуя её, что и произошло.
Сделано:

- в шаблон письма (кнопка в `kontakty.html` и в `zayavka.html`)
  добавлена строка **«Бумага и плотность»** — её не хватало, а именно
  она и определяет, по какому разделу считать;
- рядом с обычной ссылкой на почту в `kontakty.html` добавлена видимая
  подсказка: что печатаем, формат и тираж, бумага, срок, телефон.

- [ ] **Выкатить сайт** — те же `templates` целиком в `granat-site`.
      Теперь там две правки: цена сертификатов и подсказка по почте.
- [ ] **Подумать про ответ на такие письма.** Самое дешёвое — заготовка
      ответа с пятью вопросами и ссылкой на бота: бот соберёт параметры
      и пришлёт готовое ТЗ, а не переписку в почте.

### Визитки: макет в PDF, раскладка на А4, и где хранить работы

Владелица показала визитку клиента (автошкола «Старт», инструктор
Амбарян): «преобразовала файл в PNG, визитка не влезла и она размытая,
это нельзя продать, а это уже B2B-партнёры».

**Причины две, обе наши.**

1. **Макеты отдавались в PNG.** PNG и JPG — экранные форматы: буквы
   в них состоят из точек, и на бумаге текст мылит. Для печати нужен
   PDF, где текст остаётся вектором.
2. **Печатала через Epson Easy Photo Print.** Это программа для фото:
   она кадрирует изображение под лист, отсюда «не влезла».

**Что сделано.** `scripts/vizitka.py` собирает из JSON готовый к печати
PDF: обрезной 90×50 мм, припуск 2 мм, метки реза, безопасное поле 4 мм.
Вторым файлом — раскладка на А4: 10 визиток (2×5) встык, две страницы,
причём оборот **зеркально по колонкам** — иначе после переворота листа
контакты окажутся не на своей визитке, и это видно только после резки.

Данные первой визитки — `scripts/vizitki/ambaryan.json`. Новая визитка
= копия этого файла с другими данными.

**Как печатать** (записано, потому что на этом и погорели): открыть PDF
в браузере или Adobe Reader, масштаб строго «Реальный размер» / 100%,
печатать постранично и переворачивать лист слева направо. Автодуплекс
на плотной бумаге не включать. Перед партией — один лист на обычной
бумаге и линейкой проверить, что визитка ровно 90×50 мм.

- [ ] **Макеты клиентов нигде не хранятся.** Искал прежние визитки
      в обоих репозиториях — их нет: ни PNG, ни PDF. Значит работы
      живут только на ноутбуке владелицы и пропадут вместе с ним.
      Завести папку для клиентских макетов и складывать туда исходники
      и готовые PDF.
- [ ] **Печать визиток пока не наша услуга.** Принтер берёт максимум
      260 г/м² (визитки делают на 300–350), резчика нет. Макет сделать
      можем, печать — отдавать на сторону, пока не появится техника.

### Сколько заявок было на самом деле — и почему

Перед обнулением счётчика посмотрели журнал `/data`: 23 записи, из них
**настоящих клиентов двое**, остальные — тесты с номеров владелицы.

- **№2, 26.07.2026, Микаэль, 8 996 556-96-50** — «Приглашение».
- **№3, 28.07.2026, Наталья, +7 918 799-89-78** — «флаер А4
  двухсторонний», с вопросом «сколько будет стоить распечатать флаеры
  и какой минимальный заказ?». Обе доставлены. Наталья ушла к другому
  исполнителю, не дождавшись цены.

**С 28 июля до 16 августа — ни одной заявки с сайта.** Выручка июля,
со слов владелицы: печать открыток 1350 ₽ и одна страница 12 ₽.
В августе — ничего.

**Вывод, ради которого это записано: клиенты уходят не от цены,
а от её отсутствия в первый час.** Три случая подряд — Наталья в июле,
Анна сегодня, и обе с одним и тем же вопросом «сколько стоит».
Поэтому секретарь на сайте называет цену сразу, а форма заявки теперь
первой кнопкой ведёт к нему, а не в мессенджер.

- [ ] **Заявок нет, потому что на сайт никто не заходит.** Секретарь
      ждёт впустую, если не привести людей. Смотреть карточку в Яндекс
      Картах и 2ГИС: человек в Лермонтове ищет «типография рядом»
      и звонит первому в списке.

### Секретарь выкачен и проверен на живом сайте

Выкатили в 16:00, логи чистые. Прошли диалог целиком: сертификат А4,
1 штука, картон 300, срочно — **1560 ₽**, вопрос «оформляем?», ответ
«нет» → «дорого» → варианты с уже посчитанными ценами. Работает.

По ходу проверки владелица уточнила три вещи, все сделаны в тот же час:

1. **Подтверждение цены обязательно.** «Вышла цена 290 рублей. Если
   устраивает — тогда формировать заявку. Мне просто так не надо:
   я распечатала, а мне говорят — я передумала». Телефон просим только
   после «да», согласие уезжает в заявку строкой.
2. **Два ответа, без пустой переписки** — «или да, или нет».
3. **Но на «нет» секретарь доводит сам:** «не надо меня звать, сам
   секретарь доведёт диалог, спросит». Пересчитывает заказ в диалоге —
   срочность, бумага, тираж — и снова спрашивает.

**Про оплату решено так.** Обсуждали кнопку «Оплатить» и QR. Владелица
посчитала и отказалась от эквайринга: «не хочу платить сейчас банку,
срочность 290 рублей минус эквайринг минус налог». Решение верное для
нынешних сумм: с 290 ₽ комиссия и налог съедают около двадцати рублей,
плюс абонентка и настройка.

У неё **ИП на НПД** — кассу подключать не надо, чек формируется
в «Мой налог». Это записано, чтобы следующий чат не начал по новой
предлагать ККТ и фискализацию.

**Оплата доведена до конца в тот же день.** Владелица прислала PDF
с торговым QR Т-Банка. QR в файле оказался векторным поверх растровой
подложки — обычным чтением картинки не брался; отрисовали контуры
и расшифровали ссылку СБП. Она вписана в `OPLATA.ssylka`, QR рисуется
из неё при сборке (segno → SVG прямо в шаблон), проверен обратным
декодированием.

Проверено на живом сайте 16.08.2026 в 16:56: сертификат А4 на
фотобумаге, срочно — 293 ₽, предоплата 146 ₽, кнопка «Оплатить 146 ₽»,
QR под ней, перевод по номеру. Заявка №20 дошла.

Там же поймали дефект: кнопки не гасли после нажатия, и «Цену
подтвердил» записалось в чек четыре раза. Исправлено — список кнопок
очищается в момент клика.

- [ ] **Чеки на НПД.** Чек обязателен на каждый расчёт, формируется
      в «Мой налог»; при поступлении на счёт — до 9-го числа
      следующего месяца. Проверить в Т-Банке связку с «Мой налог»:
      если есть автоформирование чека при поступлении, задача
      закрывается сама. Если нет — сделать в заявке готовую ссылку
      «отправить чек в WhatsApp» с подставленным номером клиента.
- [ ] **Оферта с порядком возврата.** Сайт принимает предоплату,
      значит условия и порядок возврата должны быть опубликованы.
      Политика и согласие есть, оферты нет.
- [ ] **QR для предоплаты.** Владелица прислала картинку QR из Т-Банка,
      но вставить её в шаблон нечем: нужна ССЫЛКА, зашитую в этот QR
      (навести камеру телефона — телефон покажет адрес), либо сам файл
      картинки в репозитории. Текст блока уже согласован: «Заявка
      принята. Предоплата 50% — N ₽. Отсканируйте QR или переведите
      по номеру +7 999 244-99-99. Печать запускаем после поступления».
- [ ] **HTTPS у сайта нет.** Обнаружено при проверке в инкогнито:
      браузер пишет «Не защищено». Форма собирает телефон — по 152-ФЗ
      это передача персональных данных открытым текстом. Плюс потеря
      доверия и понижение в поиске. Чинится в Amvera бесплатно:
      `granat-site` → раздел «Домены» → выпустить сертификат.
      **Это первое, что стоит сделать завтра.**
- [ ] **Эквайринг — не сейчас.** Вернуться к вопросу, когда пойдут
      заказы на несколько тысяч, где перевод вручную неудобен клиенту.

### Секретарь на сайте — сайт наконец спрашивает сам

**Сначала о том, почему его не было.** Утром 16.08.2026 владелица уже
говорила, что после снятия Telegram-бота на сайте должен остаться
«тот самый секретарь». Соседний чат ответил, что это сейчас не главное.
Днём пришёл заказ на сертификаты, спрашивать оказалось некому, и заказ
ушёл. Её слова: «я об этом говорила утром, вот, пожалуйста, дрова
в наш адрес». Правило записано в `CLAUDE.md`, пункт 9 раздела «Как
работать с Гульнарой»: **что важно — решает она**, ответ «это сейчас
не главное» запрещён.

Заказ в итоге ушёл: «извините, мы передумали». Смена лагеря закончилась
в тот же день, сертификат нужен был к закрытию — такие заказы берутся
в первый час или не берутся вовсе.

Диагноз владелицы, её словами: **«Сайт ничего не спрашивает. Там некому
спрашивать»**. И дальше: «Раньше этим занимался бот Telegram. Сейчас
у меня телеграм не работает, мы его убрали. Но запросчик-то должен
оставаться тот самый секретарь, который хочет узнать, что вы хотите
заказать, по какой цене. Так же, как работает ВК-бот. Многие не доходят
до ВК, чтобы точно так же на сайте спрашивал».

Сделан секретарь на сайте — тот же разговор, что ведёт Джарвис
во ВКонтакте, только прямо на странице:

- `partials/sekretar.html` — чат, чек справа с растущей суммой, форма
  контакта. Подключён на главной и на странице «Печать и полиграфия».
- `partials/_js-sekretar.html` — сценарии и счёт. Вопросы и их порядок
  взяты из `CATEGORY_BRIEFS` бота, чтобы ответ в двух каналах совпадал.
  Восемь веток: сертификаты, открытки и приглашения, фото, фото на
  документы, копии, флаеры, ламинация, «другая задача».
- `scripts/sekretar-ceny.mts` → `partials/_js-sekretar-ceny.html` —
  цены. **В коде секретаря нет ни одной суммы в рублях**: всё из
  `data/prices.ts`. Пороги тиражей вычисляются из подписей столбцов,
  так что новый столбец в прайсе подхватится сам. Пересобрать:
  `npm run sekretar`.
- Заявка уходит на существующий `/api/bot-lead` — тот, что сделан для
  симулятора: он принимает контакт, собранный путь и сумму. Серверный
  код не тронут вовсе, выкатка — одна папка `templates`.
- Согласие на обработку персональных данных: галочка обязательна,
  формулировка и ссылки те же, что в форме заявки (`/consent.html`,
  `/privacy.html`), проверка продублирована в JS, и факт согласия
  уезжает в заявку отдельной строкой — по 152-ФЗ его нужно уметь
  показать.

Проверено расчётом на живых цифрах прайса: сертификат 1 шт на
фотобумаге — 225 ₽, срочно 292 ₽; на картоне — 1200 ₽ (минимальный
заказ), срочно 1560 ₽; 30 сертификатов на картоне — 5400 ₽; копия А4
ч/б 3 листа — 50 ₽ по минималке. Совпадает с ответами бота.

- [ ] **Выкатить и посмотреть глазами.** Секретарь не проверялся в
      живом браузере — только синтаксис и арифметика. После загрузки
      `templates` пройти диалог до конца и убедиться, что заявка
      доходит: в логах `granat-site` появится «ЗАЯВКА ИЗ СИМУЛЯТОРА».
- [ ] **Симулятор на `/cifra` остался прежним** — он про ботов для
      клиентов. Это нормально: там продаём разработку, здесь принимаем
      заказы. Но выглядят они похоже, и однажды их спутают.
- [ ] **Свободный текст.** Сейчас секретарь только с кнопками: надёжно
      и работает без нейросети. Если нужен разговор словами, как в ВК, —
      это отдельная работа с OpenRouter и ключом.

## 16.08.2026 — принтер Canon MF750C: удалён драйвер печати, восстановлен без скачивания

Днём: «я удалила драйвера на принтер fm750, не могу печатать». Модель
на самом деле **Canon i-SENSYS MF750C Series**, буквы в названии
переставились. Печать вернули за час, ничего не скачивая.

**Что было.** В «Принтеры и сканеры» строчка `MF750C Series` стояла с
подписью «Драйвер недоступен». При этом на странице принтера состояние
было «Простаивает», а из настроек остались только параметры **сканера**.
Значит, удалилась печатная часть драйвера, а сканер уцелел — принтер
числился в системе и был на связи, но печатать не мог.

**Чем чинили.** Скачивать драйвер Canon не понадобилось: в системе
остался универсальный `Canon Generic Plus UFR II V251` — он поддерживает
MF750C. Не хватало только связки «этот драйвер ↔ этот принтер».

Принтер подключён по Wi-Fi, адрес `192.168.1.233` (виден на его экране:
Монитор состояния → внизу строка «IP-адрес»). `ping 192.168.1.233` дал
4 ответа из 4, 0% потерь — сеть была ни при чём.

**Мастер добавления через IPP не сработал**: «ОС Windows не удаётся
подключиться к принтеру». На этом Canon способ IPP отключён. Помог
обычный порт TCP/IP, и сделали это командами, а не мышкой — так быстрее
и без блужданий по окнам Windows 11.

Порядок восстановления, три команды в чёрном окне (`cmd`):

```
powershell -Command "Get-PrinterDriver | Select-Object -ExpandProperty Name"
```

```
powershell -Command "Add-PrinterPort -Name 'IP_192.168.1.233' -PrinterHostAddress '192.168.1.233'; Add-Printer -Name 'Canon MF750C' -DriverName 'Canon Generic Plus UFR II V251' -PortName 'IP_192.168.1.233'"
```

```
rundll32 printui.dll,PrintUIEntry /k /n "Canon MF750C"
```

Первая показывает, какие драйверы есть в системе — имя из её списка
подставляется во вторую слово в слово. Третья печатает пробную страницу.
Страница вышла цветной, значит драйвер встал полностью. Отдельно принтер
сделан основным (`SetDefaultPrinter`, ответ `ReturnValue : 0`), до этого
по умолчанию стоял плоттер `Canon TM-340` и документы уходили на него.

**Наблюдение Гульнары, записано с её слов:** «сбой происходит из-за
подключения к ВПН». **Не проверено**, но правдоподобно: ВПН заворачивает
в туннель весь трафик, включая локальную сеть, и сетевой принтер по
адресу `192.168.1.233` перестаёт отвечать. Выглядит это как «принтер
пропал», хотя он стоит рядом и включён.

Чем ВПН **не** объясняется: Epson L8180 подключён шнуром USB (`USB006`),
и его «Не готово» — это питание или шнур, а не сеть. Он и до этих работ
числился «Отключен».

- [ ] **Проверить гипотезу про ВПН.** Три шага, две минуты: при
      включённом ВПН выполнить `ping 192.168.1.233`, затем выключить ВПН
      и выполнить ту же команду. Отвечает только во втором случае —
      причина найдена, и лечится она исключением локальной сети в
      настройках ВПН, а не переустановкой драйверов.
- [ ] **Не переустанавливать драйверы, пока это не проверено.** Если
      виноват ВПН, драйвер тут ни при чём, и переустановка будет лечить
      здорового.

**Тем же днём подняли и Epson.** После Canon владелица нажала «Печать» и
получила окно «EPSON L8180 Series — Не готово: автономная печать
невозможна». Выглядело как вторая поломка, оказалось — та же история с
другого конца.

Epson EcoTank **L8188** (на корпусе L8188, драйвер называется
`EPSON L8180 Series` — общий на серию) был записан в Windows на шнур USB
`USB006`, а шнур не подключён. Сам принтер при этом работал и сидел в
Wi-Fi: сеть `GulNara-5GHZ`, адрес `192.168.1.122`, канал 36, уровень
сигнала принтер считает слабым, но `ping` дал 4 из 4 по 3 мс.

Подняли одной командой, как Canon:

```
powershell -Command "Add-PrinterPort -Name 'IP_192.168.1.122' -PrinterHostAddress '192.168.1.122'; Add-Printer -Name 'Epson L8188 WiFi' -DriverName 'EPSON L8180 Series' -PortName 'IP_192.168.1.122'"
```

Пробная страница вышла: порт `IP_192.168.1.122`, поддержка цвета — да,
версия драйвера 3.80.0.0. Старую USB-запись не удаляли: пусть остаётся
на случай, если шнур когда-нибудь подключат.

**Вывод, который стоит помнить:** подпись «Отключен» под принтером
говорит не о принтере, а о том способе связи, который записан в Windows.
Оба раза железо было исправно и на связи — расходились записи в системе.

- [ ] **Сигнал Epson «Слабый».** Он в сети 5 ГГц: быстрее, но хуже
      пробивает стены. Печатать не мешает, но если пойдут срывы при
      больших фото — переключить принтер на обычную сеть «Гульнара»
      без приписки `5GHZ`, она добивает дальше. Адрес после переключения
      сменится, и порт придётся завести заново той же командой.

## 16.08.2026 — сайт лежал ночь: доехал `app.py`, не доехала папка

Утром: «У МЕНЯ УПАЛ САЙТ ГРАНАТ». Разбор занял сорок минут, из них
тридцать — поиск того, где вообще лежит код.

**Что случилось.** 15.08.2026 вечером в приложение `granat-site`
загрузили новый `app.py` — тот, из которого убран Telegram. Папку
`templates` рядом с ним не загрузили. Flask запустился нормально: все
модули на месте, gunicorn поднялся, порт слушается. Но показать страницу
ему нечем, и каждому посетителю уходила ошибка
`jinja2.exceptions.TemplateNotFound: index.html`.

**Почему заметили только утром.** Проверки, которая ловит такое, нет.
`/api/health` смотрит каналы доставки и ничего не знает про разметку:
он отвечал `ok: true`, пока сайт отдавал ошибку всем подряд. Ошибки в
логе шли каждые несколько минут — это заходили живые люди, — и никто их
не читал.

**Как чинили.** По логу. Строка `app.py, line 378` показала заодно,
что новый `app.py` встал нормально: в нём `render_template("index.html")`
стоит на 378-й строке, в старом — на 400-й. Значит, откатывать вчерашнюю
правку не надо, и gunicorn ни при чём. Владелице отправлен архив с
папкой `templates` из репозитория, она загрузила её в «Репозиторий»
и запустила пересборку. В 10:50 контейнер поднялся, ошибки прекратились,
сайт открылся.

**Проверено после починки:** `vk: ok`, `mail: ok`, `undelivered: 0` —
застрявших заявок нет, за время простоя никто ничего не оставил: люди
упирались в ошибку до формы.

**Что из этого следует:**

- [ ] **Проверка, которая ловит именно такую поломку.** `/api/health`
      должен пробовать открыть `index.html` и говорить, есть ли разметка.
      Сейчас он говорит «всё хорошо», когда сайта фактически нет.
- [ ] **Сторож снаружи.** Cron Jobs в Amvera раз в 15 минут дёргает
      главную и пишет Гульнаре в ВК, если ответ не 200. Без этого любая
      поломка живёт до первой жалобы.
- [ ] **Копию живого сайта и бота — в репозиторий.** Кнопка «Скачать
      данные» на странице «Репозиторий» каждого приложения. Сейчас
      `speech.py` не существует нигде, кроме Amvera, а остальные файлы
      бота лежат копией от 11.08 и могли отстать.

## 16.08.2026 — три памяти сведены в одну

Владелица спросила: «а почему в журналах не написано, что у нас есть бот
ВКонтакте?» Написано было — в `CLAUDE.md` ветки
`granat-kmv-multipage-site-6h04uz`, строки 96–97, и целым разделом в её
`TASKS.md`. Ассистент этой ветки не открыл, посмотрел код живого сайта,
увидел, что входящие из ВК сайт не слушает, и сказал владелице, что бота
в сообществе нет. Неверно: бот живёт не в сайте, а в приложении
`jarvis-bot`.

Причина ошибки не в невнимательности, а в устройстве: у проекта было
**три памяти в трёх ветках**, и `main` не содержал ни одной — там
лежала заготовка от 8 августа. Кто открывал репозиторий, видел её.

Сведено в `CLAUDE.md`, `STATUS.md`, `TASKS.md` и `STATISTIKA.md` одной
веткой. Правило на будущее: новых памятей не заводить, дописывать в эти.

## Хвосты по сайту на Next.js (из ветки `sozdala-fppb9e`)

Этот сайт **не опубликован** — живой работает на Flask. Хвосты оставлены
на случай, если к нему вернутся.

- [ ] Свести почту к одному адресу: в футере и `README.md` стоит
      `gulnaravibecoder999@yandex.ru`, на странице контактов —
      `info@granat-kmv.ru`.
- [ ] Страницы `/portfolio` и `/o-nas` не существуют. Ссылки на них
      убраны из меню и `sitemap.xml`, чтобы не отдавать поисковикам 404.
- [ ] Страниц `/privacy` и `/consent` нет, а форма на них ссылается и
      собирает персональные данные. По 152-ФЗ они обязаны существовать.
      На живом сайте такие страницы есть — `privacy.html` и `consent.html`.
- [ ] Развести первый экран: печать отдельно, веб отдельно. Человеку,
      которому нужны наклейки, приходится продираться через текст про
      сайты и код.
- [ ] Опечатка «корпративы» → «корпоративы», `app/pechat/page.tsx`.
- [ ] На странице контактов адрес указан как «Лермонтово». Город
      называется Лермонтов.

## Политика конфиденциальности разошлась с делом

В `privacy.html` живого сайта написано: данные «пересылаются Оператору
в мессенджер Telegram для оперативной связи с клиентом». С 15.08.2026
это неправда — заявки уходят во ВКонтакте и на почту, Telegram снят.

Документ, обещающий одно, когда происходит другое, — то, на что сошлются
в споре. Переписать абзац под фактические каналы.

Там же, мелочью: в `app.py` остался комментарий «а вопросы по задаче
задаёт человек» (около строки 710). После проверки 16.08.2026 известно,
что во ВКонтакте вопросы задаёт бот. Комментарий, который врёт, — это
следующая ошибка чьими-то чужими руками.

---

Дальше — журнал, который вёлся в ветке `granat-kmv-multipage-site-6h04uz`.
Записи оставлены как есть.


## 2ГИС: доступ получен 11.08.2026, часы оказались верными

Доступ к карточке выдали в тот же день, а не за два дня, как обещали.
Кабинет — `account.2gis.com`, вход по `gulnaravibecoder999@yandex.ru`.
Город в 2ГИС — «Пятигорск (КМВ)»: Лермонтов входит в общий проект
Кавминвод, отдельного города там нет.

**Часы в кабинете оказались правильные** — Пн–Сб 09:00–20:00,
Вс 10:00–19:00, ровно как на сайте. Внешний аудит показывал «ежедневно
10:00–18:00»: либо смотрел закэшированную версию, либо правка тогда ещё шла
через модерацию. Урок на будущее: перед тем как чинить расхождение,
найденное внешним аудитом, — сначала посмотреть в кабинете. Оно может быть
уже исправлено.

Тогда же добавлена ссылка на сообщество: `vk.com/club238836731` в разделе
«Социальные сети». Именно `vk.com`, а не `vk.me`: в справочнике человек
идёт смотреть работы и отзывы, а не сразу писать. `vk.me` остаётся на
кнопке «Написать во ВКонтакте» на сайте.

**Название тревоги не подтвердило.** В кабинете карточка называется просто
«Гранат». «Студия дизайна и вайбкодинга» в выдаче — это рубрика, а не часть
названия, менять нечего.

### Что по 2ГИС осталось

- **Проверить публичную карточку** через день-два — на `2gis.ru` через
  поиск «Гранат Лермонтов», как обычный человек. Правки идут через
  модерацию, и до неё на карточке видны старые данные. Если там 10:00–18:00
  — ждать дальше.
- **Адрес.** По внешнему аудиту в 2ГИС «Нагорная улица, 2 ст1, ТЦ, 2 этаж»,
  а у нас везде «ул. Нагорная 2/1, этаж 2». Разные записи одного адреса
  поисковик может счесть разными точками. В кабинете это поле не смотрели —
  проверить и привести к виду с сайта.
- **Проверить Telegram в карточке.** Там указан `granat_studio999` —
  убедиться, что этот аккаунт читают. Канал, куда пишут и не отвечают, хуже
  отсутствующего.
- **Отзывы.** Пять штук — слабый сигнал. Просить в момент сдачи заказа и
  давать прямую ссылку на форму. Покупать нельзя: алгоритмы вычисляют, и
  карточка теряет доверие.

Зачем это приоритет: по замеру 6–9 августа справочники дают около четверти
визитов сайта (см. `STATISTIKA.md`) — второй источник после прямых заходов.

## Цену считает нейросеть, а не калькулятор — главный незакрытый риск

В `prices.py` есть функция `quote()`: она считает цену детерминированно по
таблице, с множителями и минимальным заказом. **Диалог её не вызывает.**
Бот показывает модели прайс текстом, а арифметику модель делает сама.

10.08.2026 это дало живую ошибку в деньгах: срочный заказ на 15 фото 10×15
был оценён в 450 ₽ вместо 585 ₽. Причина — в текстовом прайсе не было
множителей вовсе, и модель просто не знала про наценку 30% за срочность.
Множители в блок добавлены, и конкретно этот случай закрыт. Но корень
остался: пока считает модель, а не код, ошибка в цене может повториться в
другом месте.

Как чинить правильно: после ответа модели пересчитывать цену через
`prices.quote()` и, если параметров хватает, показывать клиенту цифру из
кода, а не из модели. Сложность одна — модель кладёт параметры в
`collected` русскими ключами («тираж», «формат», «срочность»), а таблице
нужны `qty`, `format` и коды множителей. Нужен слой перевода имён, и его
надо проверять на живых диалогах, а не вслепую.

До тех пор считать названную ботом цену ориентировочной. Он и сам так её
называет — «ориентировочно», — но проверять сумму перед выставлением счёта
обязательно.

## Дыры: что закрыто и что осталось

Три настоящие дыры, найденные 10.08.2026 при разборе присланных ТЗ. Ни одну
из них те ТЗ не решали — они закрывали риски, которых у студии нет.

**Закрыто — телефон в форме сайта.** Поле принимало что угодно, вплоть до
пяти цифр и пробелов: заявка ложилась в журнал, а связаться было нечем.
Теперь разбирает `contact.py` — телефон приводится к `+7XXXXXXXXXX`, ник в
Telegram к `@name`, мусор возвращается клиенту с подсказкой. Заодно
проверяется, что для WhatsApp дан номер, а не ник: по нику там не написать.

Там же нашлась вторая, тихая: форма сверяла ошибку с точной строкой
«Укажите имя», а всё остальное показывало клиенту «Мы получили вашу заявку»
— хотя заявка отклонена. Человек уходил с неверным номером, уверенный, что
всё хорошо. Теперь сервер возвращает имя поля, форма подсвечивает его и
подводит к нему экран.

**Закрыто — пометка о риске в заявке.** Текст заявки прогоняется через
`fraud_check`, и при совпадениях в письме появляется блок «⚠️ В тексте
заявки есть тревожные признаки». Заявка при этом уходит ВСЕГДА. Никаких
автоотказов: у типографии с предоплатой подставная заявка ничего не крадёт,
а отклонённый по ошибке живой клиент — прямой убыток. Решает человек.

**Закрыто — голосовые.** Ни один обработчик Telegram их не ловил: клиент
наговаривал задачу и получал тишину. Теперь `speech.py` + обработчик в обоих
каналах. Порядок такой, что тишины не бывает ни при каком исходе: сначала
голос уходит Гульнаре, и только потом бот пробует его расшифровать. Не вышло
— честно говорит об этом и просит пару слов текстом. Вышло — показывает
«📝 Я услышал: …» и ведёт разговор дальше тем же путём, что и напечатанный
текст, включая проверку на мошенников.

Расшифровку показываем клиенту ДО того, как пустить в работу: распознавание
ошибается на именах и числах, а число здесь — это тираж, то есть деньги.

Во ВКонтакте сначала берём расшифровку самого ВК (она бесплатная и часто уже
готова), и только если её нет — идём в SpeechKit.

**Чтобы включить распознавание**, в переменные приложения `jarvis-bot`:
- `SPEECHKIT_API_KEY` — ключ сервисного аккаунта Яндекс Облака (пометить
  «это секрет»);
- `SPEECHKIT_FOLDER_ID` — идентификатор каталога облака.

Без ключа бот работает как раньше и просто просит написать текстом — это
рабочее состояние, а не поломка.

Граница, про которую надо помнить: быстрое распознавание берёт запись не
длиннее 30 секунд. Длинные не теряются — их пересылаем Гульнаре, а клиенту
честно говорим, что послушает человек. Длинные умеет асинхронный режим
SpeechKit, но ему нужно объектное хранилище — отдельная работа.

Почему не Whisper: из России ему нельзя ни заплатить, ни достучаться.

**Закрыто — бот не узнавал половину услуг по словам.** `prices.detect`
не понимал «визитки», «приглашения», «меню», «баннер» — а визитки самый
частый заказ. Две причины: у слов на «-ка» в родительном падеже выпадает
гласная (визит**ка** → визит**ок**), и основа их не ловила; у приглашений
основа была неверная — «приглас» вместо «пригла». Это стало критично
вместе с голосовыми: наговоренный заказ проходит ровно через эту функцию.
Добавлены визитки, меню, широкоформат и веб-направление; «сайт-визитка»
теперь уходит в сайты, а не в печать визиток. Проверка — `python prices.py`.

**Осталось — заявки негде посмотреть.** Журнал лежит файлом `leads.jsonl` на
диске Amvera, и Гульнара его не видит. Нужна выгрузка в Google-таблицу,
которую можно открыть с телефона. Одна страница, без базы данных.

## Проверка мошенников — выложить в бота

Джарвис умеет разбирать мошеннические сообщения. Работает в обоих каналах
одинаково: и в Telegram, и во ВКонтакте.

**Как позвать.** Написать боту «мошенники» — включается режим проверки.
Дальше туда можно кидать любой подозрительный текст или ссылку. Выйти —
слово «меню». В Telegram есть ещё команда `/check`.
Если деньги уже ушли — написать «меня обманули»: бот выдаст порядок
действий (банк → письменное заявление → полиция → кредитная история).

Отдельно: если в обычном сообщении набор классических приёмов, бот
вмешается сам, без всякой команды. Это на случай, когда просто
пересылаешь текст, не вспомнив нужное слово.

**Файлы.** `fraud_check.py` — все правила и ответы, общий мозг для двух
каналов. `vk_jarvis.py` и `jarvis_bot.py` — по десятку строк, которые его
зовут. Ни нейросети, ни интернета: разбор идёт по таблице правил прямо в
файле, поэтому работает ночью и при мёртвом OpenRouter.

**Выложить в бота** (приложение `jarvis-bot`, не сайт — см. ловушку ниже):

```
git fetch origin claude/granat-kmv-multipage-site-6h04uz
git show origin/claude/granat-kmv-multipage-site-6h04uz:fraud_check.py > fraud_check.py
git show origin/claude/granat-kmv-multipage-site-6h04uz:vk_jarvis.py > vk_jarvis.py
git show origin/claude/granat-kmv-multipage-site-6h04uz:jarvis_bot.py > jarvis_bot.py
```

Затем загрузить эти три файла в приложение `jarvis-bot` и перезапустить.
Проверить, что версия новая: `findstr /C:"fraud_check" jarvis_bot.py` —
должна найтись строка. Латиницей, кириллицу консоль ищет плохо.

**Проверить в бою.** Написать боту «мошенники», потом вставить туда:
«Служба безопасности банка, срочно назовите код из смс» — должно прийти
красное «ЭТО ОБМАН» с разбором. Потом написать «меню» — вернуться к заказу.

**Правки правил.** Все признаки лежат списком в начале `fraud_check.py`.
После любой правки запустить `python fraud_check.py` — он сам прогонит
дюжину эталонных случаев и скажет, не сломалось ли. Половина эталонов —
обычные заказы клиентов: на них тревожиться нельзя, иначе предупреждения
перестанут читать.

## Сменить токен бота «Генерал» — обязательно

09.08.2026 токен несколько часов был виден на открытой странице
`/api/health`: проверка ботов отдавала текст сетевой ошибки как есть, а
Telegram кладёт в него полный адрес запроса вместе с токеном. Утечку
закрыли в тот же день (коммит «Токен бота больше не утекает в /api/health»),
но сам токен считаем скомпрометированным: кто успел его скопировать, тот
может читать заявки.

Что сделать, когда откроется Telegram:
1. @BotFather → `/revoke` → выбрать бота → получить новый токен.
2. Amvera → granat-site → Переменные окружения → `TELEGRAM_BOT_TOKEN`
   (и `ADMIN_BOT_TOKEN`, если заведён) → вставить новый.
3. Открыть `/api/health` и убедиться, что `telegram: ok`.

Старый токен после `/revoke` перестаёт работать сам — отдельно ничего
отключать не нужно.

## Восстановить канал ВКонтакте — у сайта сделано, проверено 16.08.2026

`/api/health` живого сайта отвечает `vk: ok` и `mail: ok`. Ключ
сообщества у приложения `granat-site` действующий, заявка уходит двумя
каналами. Telegram снят 15.08.2026 намеренно, третьего канала больше нет.

Осталось проверить то же самое у **`jarvis-bot`**: у него свой набор
переменных, и `VK_TOKEN` там задаётся отдельно. Рабочий ключ у сайта не
означает рабочего ключа у бота.

Ниже — как чинить, если `vk` снова покажет
`5 User authorization failed: invalid access_token`:
1. vk.com → сообщество → Управление → Работа с API → Ключи доступа →
   создать ключ с правом «сообщения сообщества».
2. Amvera → Переменные окружения → `VK_TOKEN` → вставить новый.
3. `/api/health` → `vk` должен стать `ok`.

Если после этого появится ошибка 901 — значит с личной страницы ни разу
не писали сообществу. Достаточно написать ему любое сообщение.

## Два приложения — держать в голове

`granat-site` (сайт) и `jarvis-bot` (Джарвис) — РАЗНЫЕ приложения Amvera,
у каждого свой git и свой диск `/data`. Журнал заявок лежит на диске
сайта, и бот прочитать его не может физически. Всё, что им нужно друг от
друга, ходит по сети через `/api/lead/...` — см. «Мостик к Джарвису» в
`app.py`.

Файлы бота (`jarvis_bot.py`, `lead_handoff.py`, `dialog_manager.py` и
прочие) лежат и здесь, и в репозитории `jarvis-bot`. Правка в этом
репозитории САМА В БОТА НЕ ПОПАДАЕТ — файл нужно загрузить в приложение
`jarvis-bot` через его страницу «Репозиторий». Копия здесь однажды уже
отстала от рабочей на десятки строк.

Чтобы мостик работал, в переменных должно быть задано:
- `granat-site`: `WATCH_TOKEN` — любая длинная случайная строка.
- `jarvis-bot`: `SITE_URL` = `https://granat-kmv.ru`, `SITE_TOKEN` = то же
  значение, что `WATCH_TOKEN`.

Пока `SITE_TOKEN` не задан, Джарвис молча работает по-старому — с нуля.

Переменные Amvera подхватывает только при старте приложения: завёл — нажми
перезапуск, иначе получишь `forbidden` на ровном месте.

Проверка мостика без бота, прямо в браузере:
`granat-kmv.ru/api/lead/<номер>?token=<WATCH_TOKEN>` — должна вернуться
заявка. 403 значит, что ключи не совпали или приложение не перезапускали.

Проверка целиком (нужен работающий Telegram): оставить заявку на сайте,
нажать «Продолжить в Telegram» и увидеть «вижу вашу заявку №N» вместо
вопроса про согласие. 09.08.2026 этот шаг не прошли — Telegram лежал.

## Джарвис во ВКонтакте — отвечает, проверено 16.08.2026

Первый живой диалог прошёл: на «привет, нужна ксерокопия» бот ответил
за секунды, спросил формат, принял «А4», пошёл к цветности. Работает
досбор параметров, а не заготовленный ответ.

Осталось пройти путь **до конца** — до подтверждения ТЗ и до того, что
готовое ТЗ приходит Гульнаре. Один диалог, но пока не сделан.

Заодно снят вопрос, ради которого раздел заводился: сайт с 15.08 уводит
клиентов во ВКонтакте, и теперь точно известно, что там их встречают.

Устройство: отдельный поток рядом с
Telegram-ботом, долгий опрос сообщений сообщества, движок диалога общий —
тот самый `dialog_manager`. Правка промпта меняет оба канала сразу.

Чего в ВК-версии сознательно нет:
- Подтверждения телефона. Во ВКонтакте нет кнопки «поделиться номером»,
  проверить номер нечем. Вместо этого в заявку кладётся ссылка на
  страницу клиента — по ней и отвечаем.
- Апсейл-подарков. Сначала пусть заработает главное.

Переменные приложения `jarvis-bot`:
- `VK_TOKEN` — тот же ключ сообщества, что у сайта.
- `VK_PEER_ID` — id страницы Гульнары (1088419855), куда падает готовое ТЗ.
- `VK_GROUP_ID` — 238836731, можно не задавать: это значение по умолчанию.

Без `VK_TOKEN` модуль молча не запускается, Telegram работает как раньше.

Что проверить: написать сообществу с другой страницы, пройти путь до
конца и убедиться, что ТЗ пришло. В логах `jarvis-bot` при старте должна
быть строка «🔵 ВКонтакте: слушает сообщения сообщества».

После проверки — снять с экрана заявки оговорку «во ВКонтакте отвечает
человек»: она станет неправдой (`follow_up` в `app.py`).

## Обнулить счётчик заявок после тестирования

Сейчас в нумерации сидят тестовые заявки (№11–15). Когда проверки
закончатся, счётчик сбросить, чтобы первая настоящая заявка была №1.

Где лежит: постоянный диск Amvera, каталог `/data`.
- `lead_counter` — сам номер, одно число.
- `leads.jsonl` — журнал событий по заявкам.

Как сбросить: Amvera → granat-site → Данные → удалить `lead_counter`.
Следующая заявка получит №1: `next_id()` заводит файл заново.

Журнал `leads.jsonl` при этом лучше не трогать без нужды — по нему бот
находит клиента по телефону и связывает заявку с перепиской. Если решено
чистить и его, надо понимать, что старые привязки к чатам пропадут.

## SEO: страницы под отдельные услуги

Технический минимум на сайте уже стоит: уникальные title и description по
страницам, canonical, OG-теги, lang, viewport, robots. Переделывать нечего.

Не хватает двух вещей:

1. Отдельные OG-картинки. Сейчас у всех страниц одна — логотип, и ссылка
   на приглашения выглядит в мессенджере так же безлико, как ссылка на
   ботов. Нужны горизонтальные макеты с готовой работой и подписью.

2. Страниц под отдельные услуги нет: всё сведено в «печать» и «цифру».
   Начинать с трёх, а не с восьми — визитки, приглашения, меню: самые
   частые запросы, и по ним есть готовые работы для портфолио.

Делать с текстами, а не с одними мета-тегами: пустая страница с идеальным
title в поиске не удержится.

## Сделано 09.08.2026

- Заявка уходит третьим каналом — письмом на Яндекс. Для этого в Яндекс ID
  заведён пароль приложения, а в почте включён доступ почтовым программам
  (Настройки → Почтовые программы → IMAP). Без этой галочки Яндекс
  отклоняет вход даже с верным паролем приложения.
- Каналы бьют одновременно, а не по очереди: форма больше не висит минуту,
  когда один из каналов молчит.
- Проверки в `/api/health` перестали быть строже боевой отправки и больше
  не дают ложную тревогу по Telegram.

### 17 августа, ночь — вся работа сведена в main, ветки больше не расходятся

**Решение владелицы:** свести сегодняшнюю работу в `main`.

Сделано: `main` переведён с `67362e5` на `a9314e4`, перемоткой вперёд,
без слияния — рабочая ветка шла ровно на десять коммитов впереди и ни
на один позади. Терять было нечего.

**Зачем.** Внешний аналитик читал файлы по ссылке с полным хэшем
коммита, потому что в `main` сегодняшнего не было. Такая ссылка живёт
до следующей правки. Теперь и он, и любой новый чат читают просто
`main`.

Проверено: шесть ключевых файлов отдаются из `main` по прямой ссылке —
`data/prices.ts`, `docs/pozicionirovanie.md`, `CLAUDE.md`,
`STATISTIKA.md`, `tasks/market-research/SHABLON.md`,
`.github/ISSUE_TEMPLATE/marketing.md`.

**Что теперь в `main`, чего не было утром:**
- секретарь с вопросами про формат, стороны и макет;
- ступени биговки 7 / 3 / 2 ₽ за биг;
- сертификат от одной штуки, розничный порог 150 ₽;
- предоплата 30 % вместо 50 % во всём прайсе;
- визитки, наклейки и брошюровка заведены в прайс;
- разбор итога в диалоге (база, срочность рублями, итого);
- `npm run brief` — досье для рыночного аналитика;
- `npm run proverka` — проверка текстов объявлений перед публикацией;
- очередь маркетинговых задач в GitHub Issues с шаблоном и метками;
- `docs/pozicionirovanie.md` — пять отличий с подтверждением в коде.

**Заведён первый Issue:** № 1 «Цена грамоты А4 разъехалась вдвое»,
метка `needs-owner`. 150 ₽ по разбору 15.08 против 200 ₽, которые
считает бот; на тираже 30 штук расхождение 2 100 ₽. В работу не идёт
до решения владелицы.

**Ждём от Гульнары:**
- [ ] Выбрать вариант в Issue № 1 — цена грамоты А4.
- [ ] **Выкатить сегодняшние правки на живой сайт.** Сам сайт РАБОТАЕТ —
      подняли 16.08 в 10:50, владелица открыла и проверила 17.08 ночью.
      Но он крутит версию от 16.08: сегодняшние правки секретаря лежат
      только в GitHub. Загрузить в `granat-site` папку `templates`
      целиком (не файлами по одному — на этом уже потеряли ночь 15.08).
- [ ] Решить, закрывать ли репозиторий. Он открытый, и `data/prices.ts`
      с комментариями о себестоимости читает кто угодно. Связке
      с аналитиком закрытие не помешает: ему уходит клиентский прайс
      через `npm run brief`, а не исходник.

**Остановились на:** репозиторий приведён в порядок, `main` свежий,
очередь задач заведена. Осталось выкатить правки в Amvera.

**ПОПРАВКА, сделана тут же по замечанию владелицы.** Выше по тексту
и в календаре у меня стояло «живой сайт лежит с 16.08, 500-я ошибка».
Это неправда: сайт подняли 16.08 в 10:50, о чём есть запись в этом же
журнале и коммиты того же дня «проверена на живом сайте» (`6a3976d`,
13:57) и перевод на https (`6d4df3c`, 14:27). Владелица открыла сайт
ночью 17.08 — работает.

Ошибка моя: я взял факт из ранней части беседы, где сайт действительно
отдавал 500-ю — это было утро 16-го, ДО починки, — и не перепроверил.
Повторил в трёх местах: в журнале, в календаре и в ответах владелице.
Проверка занимала минуту:

```
git log --all --since='2 days ago' --format='%h %cd %s' --date=format:'%d.%m %H:%M'
```

Правило из `CLAUDE.md` про это есть, и оно шире, чем записано: **факт
из середины беседы стареет так же, как запись в журнале.** Живой сайт
и история коммитов старше и того, и другого.

### 17 августа — первая проверка рынка КМВ. Два наших вывода оказались догадками

Аналитик ответил на три вопроса из `VOPROSY-ANALITIKU.md`. Ответ
со ссылками на прайсы, датами проверки и честным «не найдено» вместо
выдуманных цифр — первый его ответ, к которому нет претензий. Отдельно
оговорил, где цифра посчитана им, а не взята из прайса (ступени 96
и 192 штуки у «ОптПолиграфа»).

**Что проверка сломала — два вывода из вчерашнего позиционирования:**

1. **«От одной штуки» — НЕ уникальное отличие.** Я записал это как
   «прямое попадание в боль „отказали в маленьком заказе“». РПК «Кратос»
   в Пятигорске печатает А4 **от одного экземпляра**, 60 ₽ за лист.
   Отличие остаётся своим условием, но «единственные, кто печатает
   от штуки» — формулировка запрещённая.
2. **Печать файлов из нейросети снята с роли главного отличия.**
   Подтвердилось: не обещает никто. Но отзывов «принёс картинку,
   отказались печатать» тоже не нашлось. **Нет боли — нет оффера.**
   Остаётся второй строкой как гипотеза на аккуратный тест.

Оба вывода я построил вчера из того, что мы умеем, — и оба звучали
убедительно. Проверка стоила одного сообщения.

**Что проверка нашла нового — визитки.** Мы дороже рынка на бумаге
тоньше: 7,5 ₽ за штуку против 6,5 ₽ у «ОптПолиграфа», и у них 300 г/м²
против наших 250–260. 300 — отраслевой стандарт, на нём печатают
все четверо найденных конкурентов. Заведён
[Issue № 2](https://github.com/gulnaragold999m-tech/granat-kmv/issues/2),
метка `needs-owner`, четыре варианта решения.

**По грамоте рынок вопрос не решил.** Готовую грамоту А4 на плотном
материале с открытой ценой в КМВ не публикует никто. Значит
[Issue № 1](https://github.com/gulnaragold999m-tech/granat-kmv/issues/1)
решается по своей экономике: материал, износ, время, риск штучного
запуска, маржа. Комментарий с разбором добавлен в Issue.

**Сделано:**
- [x] `9156db9` поправка в журнал: сайт работает, лежащим он не был
- [x] Ответы аналитика записаны в `tasks/market-research/VOPROSY-ANALITIKU.md`,
      три вопроса закрыты
- [x] `docs/pozicionirovanie.md`: два вывода ослаблены, конкуренты
      заменены на проверенных со ссылками на прайсы
- [x] Issue № 2 заведён, в Issue № 1 добавлен комментарий с проверкой

**Правило, которое из этого следует.** Вывод, построенный из своих
возможностей, — это гипотеза, а не отличие. Отличием он становится
только после того, как проверено: делают ли так конкуренты **и есть ли
у клиентов такая боль**. Второе важнее первого. Уникальность без боли
никому не нужна.

**Ждём от Гульнары:**
- [ ] Issue № 1 — цена грамоты А4, три варианта.
- [ ] Issue № 2 — цена визиток, четыре варианта.
- [ ] Открыть карточки конкурентов в 2ГИС глазами: часы, отзывы, ответы
      владельца на негатив. В прайсах этого не видно.

### 17 августа — бумага и принтеры: в прайсе одно число на четыре машины

Владелица принесла разбор, почему Epson не берёт картон 300 г/м², хотя
в паспорте написано «до 300». Разбор верный, и он вскрыл дыру в нашем
прайсе.

**Дело не в весе, а в жёсткости.** Ролик захвата у струйного Epson
рассчитан на гибкий материал: фотобумага 250 г/м² проходит, мелованный
картон той же плотности — нет. Плюс чернила на мелованной поверхности
не сохнут, у неё нет впитывающего слоя.

**Наша дыра.** Вчера я поставил в прайс `MAKSIMALNAYA_PLOTNOST = 260`
и подпись «принтер не берёт плотнее». **Какой принтер — в коде
не сказано, а их четыре.** При этом прайс обещает открытки
и сертификаты на картоне 250–260 и считает по ним цену. Если картон
берёт только лазерный Canon — это надо записать. Если не берёт ни один —
мы обещаем то же самое, что вчера обещали с 300 г/м², только на сорок
граммов ниже.

**Решение владелицы:** перейти на матовую ДВУСТОРОННЮЮ фотобумагу
220–250 г/м². Она гибкая, принтер её берёт, а по матовому обороту
пишут обычной ручкой — у глянцевой фотобумаги оборот пластиковый
и ручка мажет.

**Сделано:**
- [x] `docs/bumaga-i-printery.md` — таблица техники, почему паспортные
      300 не работают, спецификация бумаги, что проверить на первой
      пачке, и отложенный совет про смену принтера.
- [x] Оговорка в `data/prices.ts` рядом с `MAKSIMALNAYA_PLOTNOST`:
      одно число на четыре машины, решает пара «плотность плюс
      жёсткость», вопрос про картон открыт.
- [x] Пометка в секретаре: правило «фотобумага исчезает при двусторонней
      печати» ВРЕМЕННОЕ, снимается когда куплена двусторонняя пачка.

**Чего сознательно НЕ сделали.**

Правило про фотобумагу не сняли. Двусторонняя бумага решает вопрос,
но пока на полке односторонняя — правило защищает клиента от заказа,
который размажется. Меняем после покупки и проверки, не раньше.

Бренды (Lomond, Privision, Aero) записаны как названные советчиком
и непроверенные. Покупать по спецификации — «матовая, двусторонняя,
220–250 г/м², для струйной печати», — а бренд брать тот, что есть.
Ошибиться можно в одном месте: взять одностороннюю вместо двусторонней.

Совет продать Epson и купить б/у OKI за 40–100 тысяч отложен. Мы
не знаем, сколько открыток на жёстком картоне продаём в месяц; сейчас
это ноль. Покупать технику под неизмеренный спрос — то же самое, за что
уже заплатили рекламой. Секретарь пишет причину отказа в заявку, так
что данные накопятся сами.

**Ждём от Гульнары — два вопроса, оба про технику:**
- [ ] **Открытки и сертификаты на картоне 250 г/м² печатаются на каком
      принтере?** На Canon MF750C или на Epson? И берёт ли Canon картон
      250 без замятий? От ответа зависит, честен ли раздел открыток
      в прайсе.
- [ ] Матовая фотобумага 230, которая лежит сейчас, — односторонняя
      или двусторонняя? На пачке пишут «Double Sided».

### 17 августа, 10:20 — ВЫКАЧЕНО НА ЖИВОЙ САЙТ. Проверено глазами

Владелица прошла заказ на `granat-kmv.ru` и прислала экран. Всё
сегодняшнее видно клиенту:

```
Носитель: Плотная матовая бумага 250–260 г/м²
Отделка: Без отделки
Макет: Сделал сам, в нейросети
Срок: Сегодня, день в день

Стоимость заказа            200 ₽
Срочность: сегодня — +30%   +60 ₽
Итого                       260 ₽

Печать запускаем после предоплаты 30%.
```

**260 ₽ вместо 1 200 ₽.** Это ровно тот заказ, за которым звонил первый
живой клиент с Карт 16 августа: один сертификат А4, срочно. Утром бот
назвал бы 1 200 ₽ — минимум раздела открыток накрывал сертификаты.

**Как выкатывали** (порядок пригодится в следующий раз):

1. `git fetch origin main`
2. `git checkout origin/main -- zhivoj-sajt`
3. `findstr /C:"shagOtdelka" zhivoj-sajt\templates\partials\_js-sekretar.html`
   — проверка, что файлы свежие. Слово латиницей, кириллицу консоль
   ищет плохо.
4. Amvera → granat-site → Репозиторий → «Загрузить данные» →
   **перетащить папку `templates` ЦЕЛИКОМ** в пунктирную область.
5. Проверить в списке «Загруженные файлы», что пути начинаются
   с `/templates/` и среди них есть `/templates/partials/`.
6. «Закрыть» → перезапуск приложения.

**Что выяснилось про Amvera:**
- Окно загрузки принимает папки перетаскиванием, zip не принимает,
  предел 200 МБ.
- Подпись к коммиту не обязательна, на работу не влияет.
- **Перезапуска хватило, пересборка не понадобилась.**
- Перед перетаскиванием обязательно выйти в КОРЕНЬ репозитория:
  бросить `templates` изнутри `templates` — получится `templates/templates`,
  и сайт ляжет, как 15 августа.

**Проверено на живом сайте:**
- [x] «картон» заменён на плотную матовую бумагу
- [x] вопрос «Добавим отделку?» появился
- [x] вопрос про макет с веткой «сделал сам, в нейросети»
- [x] разбор итога: база, срочность рублями, итого
- [x] предоплата 30 %
- [x] сертификат от одной штуки — 260 ₽ вместо 1 200 ₽

**Осталось от Гульнары:**
- [ ] Issue № 1 — цена грамоты А4: 150 или 200 ₽.
- [ ] Issue № 2 — цена визиток: выше рынка на бумаге тоньше.
- [ ] Перепечатать шпаргалку у стойки: старая обещает картон 300–350 г/м².

### 17 августа, 14:02 — материал спрашивается везде. Выкачено и проверено

Владелица прошла заказ на живом сайте: листовки А4, 500 шт, бумага
160 г/м² — **13 440 ₽**. До сегодняшнего дня бот назвал бы 12 000:
наценку за плотность он не спрашивал.

**Как нашлось.** Пришёл живой клиент из ВКонтакте — витражная
мастерская, 500 листовок А4. Владелица назвала 12 000 ₽ по прайсу,
а потом сказала: «у меня правда бумага плотность 160 грамм». И следом:
**«Надо чётко: для бумаги 130 грамм одна цена, для 150 другая, для 160
третья. Иначе я буду терять свои деньги постоянно»**.

Она была права, и дыра оказалась шире одного заказа.

**ПРАВИЛО, ради которого эта запись:**

> **Наценка, которую не спрашивают кнопкой, не существует.**

Наценка за плотность лежала в прайсе с самого начала. Секретарь
печатал её примечанием — «плотнее бумага плюс 10%» — клиент примечание
не выбирал, и в счёт наценка не попадала НИ РАЗУ. То же самое было
с премиальной фотобумагой: +25% в прайсе, ноль в кассе.

**Второе правило, словами владелицы:** «Там хоть открытка, хоть
сертификат, хоть фотография — всегда надо спрашивать».

**Сделано:**
- [x] `5e19352` листовки: пять плотностей, на каждой кнопке готовая
      сумма за тираж, а не процент. 12 000 / 13 200 / 13 440 / 13 800 / 15 000
- [x] `5e19352` открытки и приглашения: три бумаги вместо одной жёстко
      зашитой. А6 100 шт со сгибом — 3 350 / 3 700 / 4 400
- [x] `5e19352` фотографии: обычная и премиальная плотная.
      10×15, 50 шт — 1 250 / 1 563
- [x] Плотность 160 г/м² заведена в прайс, наценка 12 % — интерполяция
      между 150 (+10 %) и 170 (+15 %), **владелице подтвердить**
- [x] Выкачено на живой сайт и проверено владелицей в 14:02

**Теперь материал спрашивают все разделы, где он влияет на цену:**
сертификаты, открытки, фотографии, листовки. У копий, ламинации
и фото на документы материал на цену не влияет — там своя таблица.

**Ждём от Гульнары:**
- [ ] Подтвердить наценку 12 % для 160 г/м² или назвать свою.
- [ ] Витражам: назвали 12 000 ₽ по 130-й бумаге, печать будет
      на 160-й — это 13 440 ₽. Разницу 1 440 ₽ решено подарить как
      первому клиенту. **Подарок назвать вслух**, иначе его не заметят.

### 17 августа, 14:05 — поле «приложите макет» подтверждено на живом сайте

Владелица открыла `www.granat-kmv.ru`, дошла в секретаре до последнего
шага и увидела своими глазами: телефон, согласие на обработку данных
и **«Макет, если есть — приложите сразу»** с кнопкой выбора файла
и подписью «PDF, JPG, PNG и другие. До 20 МБ. Необязательно.».
Её слова: «и есть куда макет приложить».

Этим закрывается круг, начатый её же наблюдением сегодня утром:
«Здесь нет возможности приложить документ, отправить на почту, чтобы
человек не выдумывал ничего, не искал какую ещё форму заполнить.
Он уже согласен, и с расценками у него в голове всё решилось.
Осталось приложить документ и оплатить предоплату».

**Сделано и проверено:**
- [x] `6f5f25a` поле файла в секретаре, отправка через FormData
- [x] `6f5f25a` `app.py`: макет сохраняется на диск `/data/makety` ДО
      отправки письма и уходит вложением на почту. В ВК — только текст
- [x] Проверено владелицей на живом сайте в 14:05

**Чего у этой правки НЕТ, и это осознанно:** `.html` и `.svg`
в список разрешённых расширений не входят — они исполняются
в браузере, а файлы лежат на нашем же домене.

**Оба подтверждения дня — 14:02 и 14:05 — от владелицы, не от логов.**
Правило прежнее: сделано то, что видно клиенту, а не то, что
закоммичено.

### 17 августа, 12:30 — логотип для 2ГИС. Заведена папка фирменного стиля

Владелица: «мне для дела нужен логотип, пусть будет» и следом —
«мне нужен такой логотип, чтобы отправить в 2ГИС, и чтобы там буквы
все были читаемы».

**Что выяснилось по дороге, и это важнее самой картинки.**

1. **Логотипа не было в репозитории ни в каком виде.** При этом на
   сайте он работает с самого начала: шапка, подвал, первый экран,
   значок вкладки, значок на телефоне, картинка при пересылке ссылки
   в мессенджер и логотип организации в микроразметке — всё это один
   файл `logo.png` в корне приложения `granat-site`. Он существовал
   **в одном экземпляре, на диске Amvera**. Та же дыра, что с
   `speech.py`.
2. **2ГИС вписывает логотип в круг** и показывает его размером около
   200×200 точек. Углы квадрата обрезаются. Исходный знак с тремя
   ярусами — дуга, монограмма в рамке, название — на таком размере
   сливается в пятно.

**Сделано:**
- [x] `57c4259` заведена папка `firmennyj-stil/` с картой: где логотип
      используется на сайте (семь мест из одного файла), каких версий
      не хватает для печати, чем грозит единственный экземпляр
- [x] `57c4259` `granat-logo-2gis.svg` — вектор: кольцо, «ГРАНАТ»
      крупно, «ДИЗАЙНЕРСКАЯ СТУДИЯ» по дуге. Монограмма снята
      намеренно, ради читаемости в кружке
- [x] `57c4259` `granat-logo-2gis-1500.png` — 1500×1500, под требования
      2ГИС: круг 1400 точек при минимуме 200, объём 0,9 МБ при пределе 10
- [x] `57c4259` проба 200×200 с круглой обрезкой — проверка глазами
      в том размере, в котором логотип реально видит человек
- [x] `57c4259` `sobrat.sh` — пересборка PNG из вектора

**Грабли рендера, чтобы не наступить снова.** Обычный `chrome
--headless --screenshot` даёт окно на 85 точек выше области рисования,
и снизу картинки остаётся белая полоса — первый файл вышел с браком.
Рисовать надо бинарником `headless_shell`: у него окна нет вовсе,
снимок совпадает с кадром. Записано в `sobrat.sh`.

**Правило, ради которого эта запись:**

> **Картинка, существующая в одном экземпляре, — это не актив, а риск.**

Логотип прожил так девять дней, и заметили случайно.

**Ждём от Гульнары:**
- [ ] Загрузить логотип в карточку 2ГИС, потом в Яндекс Бизнес.
- [ ] Положить в `firmennyj-stil/` ОРИГИНАЛ логотипа, если он есть
      в векторе (`.ai`, `.cdr`, `.eps`, `.svg`). Без него любая печать
      крупнее визитки будет с рваными краями.

### 17 августа, 13:10 — вензель нашёлся. Он существует только картинкой

Владелица прислала вензель «G» и сказала: «эту букву уже рисовали для
моих сайтов, просто посмотри как это делали». Посмотрел по обоим
репозиториям.

**Ответ: её нигде не рисовали.** Ни кодом, ни вектором. Это растровый
файл `public/assets/logo-g.png` в репозитории студии — **299×400 точек**,
PNG с прозрачностью, 62 КБ. Вторая копия рядом, `public/logo-mark.png`,
256×256. Векторного исходника нет ни в `vibe-digital999`, ни в
`granat-kmv`.

**Где он используется на сайте студии:** картинка при пересылке ссылки
(`og:image`), логотип организации в микроразметке Schema.org, значок
на первом экране и заглушка в карточке-подсказке. То есть знак рабочий
и живёт во всех витринах — но в единственном растровом виде.

**Чем это ограничивает.** Крупнее 400 точек он мылится. На визитке
пройдёт, на баннере или табличке — нет. Перерисовать в вектор: обвести
по контуру, час-полтора работы, и после этого он печатается любого
размера навсегда.

**Заодно всплыл незакрытый вопрос от 11.08** (`vibe-digital999/TASKS.md`):
какой знак у студии один — вензель «G» (наследство «Граната») или «V»
по имени и домену. Решение так и не принято, и на макетах до сих пор
разнобой.

**Сделано:**
- [x] `62f7509` `venzel-g.png` скопирован в `firmennyj-stil/` — до сегодня
      знак «Граната» лежал только в репозитории студии
- [x] `62f7509` второй вариант логотипа — с вензелем: сам вензель вшит
      в SVG как base64, файл самодостаточный
- [x] `62f7509` проба 200×200 для обоих вариантов, рядом для сравнения

**Вывод по 2ГИС: брать вариант БЕЗ вензеля.** На пробе в 200 точек
вензель превращается в золотое пятно — прочесть на его месте нечего,
а «ГРАНАТ» он заставляет уменьшить. Вариант с вензелем — для сайта,
макетов и печати, где логотип показывают крупно.

**Ждём от Гульнары:**
- [ ] Выбрать вариант для 2ГИС. Моя рекомендация — без вензеля.
- [ ] Решить, нужен ли вензель в векторе. Нужен — это отдельная работа
      на час-полтора, зато один раз и навсегда.

### 17 августа, 13:25 — логотип набран фирменным шрифтом. И находка про сайт

Владелица: «а буквы наши возьми» и «какой шрифт использовали?».

**Отвечаю на второй вопрос честно: в первой версии логотипа стоял
не ваш шрифт.** DejaVu Serif — тот, что нашёлся в моём окружении.
Похож по духу, но чужой.

**Какие шрифты ваши.** Заданы в `zhivoj-sajt/templates/base.html`:
**Playfair Display** для заголовков и слова «Гранат» в шапке (класс
`.studio-logo-gold`, разрядка `.18em`), **Montserrat** для обычного
текста. Оба скачаны кириллическими подмножествами и вшиты в вектор.

**И вот находка, ради которой стоило спросить.**

> **На живом сайте эти шрифты НЕ подключены.**

В CSS они названы, а самого шрифта нет: ни `@font-face`, ни ссылки
на Google Fonts — проверено и в `base.html`, и в `static/css/tailwind.css`.
Браузер молча берёт запасной. На Windows заголовки и слово «Гранат»
показываются шрифтом **Georgia**, обычный текст — системным Arial.

Это не поломка, сайт работает. Но фирменного шрифта на нём нет,
и не было никогда. Заметить это можно было только так — попытавшись
взять «наши буквы» и полезши в код за ними.

**Сделано:**
- [x] `92996e0` Playfair Display 700 и Montserrat 600, кириллица,
      по 11–12 КБ — в `firmennyj-stil/shrifty/`
- [x] `92996e0` `sobrat.mjs` вместо `sobrat.sh`: собирает разом шесть
      файлов — два вектора, два PNG по 1500, две пробы по 200.
      Шрифты вшиваются в SVG как base64, файл самодостаточный
- [x] `92996e0` оба варианта логотипа пересобраны фирменным шрифтом

**Почему сборка скриптом, а не руками.** Файлов шесть, и правка размера
должна доехать до всех. Разъедутся — получим ту же историю, что с ценой
на макетах: на сайте одно, в карточке другое.

**Ждём от Гульнары:**
- [ ] Подключить шрифты на сайте — отдельная работа, правка `base.html`
      плюс выкатка в Amvera. Делать или оставить Georgia — решение её.

### 17 августа, 13:40 — владелица выбрала вариант с вензелем

Её слова: «самый лучший из файлов второй». Это
`granat-logo-2gis-s-venzelem` — тот, где вензель «G» стоит над названием.

**Рекомендация ассистента была другая** — без вензеля, потому что
в кружке 2ГИС на 200 точках вензель читается плохо. Возражение
высказано один раз, владелица выбрала. **Решение принято, к нему
не возвращаться.** Знак важнее удобства чтения на самом мелком
размере — это нормальный выбор для фирменного стиля, а не ошибка.

**Сделано:**
- [x] `af258aa` название в варианте с вензелем поднято со 168 до 196 —
      предел, дальше слово упирается в кольцо. Вензель остаётся,
      а «ГРАНАТ» читается настолько, насколько возможно
- [x] `af258aa` в `README.md` записано: основной — с вензелем, обратно
      не «чинить»

Вариант без вензеля не удалён: пригодится для значка вкладки и мелких
аватарок.

**Ждём от Гульнары:**
- [ ] Загрузить в 2ГИС, потом в Яндекс Бизнес и на аватарку ВКонтакте.

### 17 августа, 14:00 — слоган по нижней дуге. Четыре варианта на выбор

Владелица: «может слоган написать внизу также как дизайнерская студия».

**Первым делом проверил, нет ли слогана уже.** Есть, и он на живом
сайте — `zhivoj-sajt/templates/partials/hero.html`, золотая строка
под заголовком: **«Ваш бизнес запомнят с первого взгляда»**. Сочинять
новый поверх готового не стал. Тот же урок, что со шрифтом: сначала
смотрим, что есть, потом рисуем.

**Грабли, которые вылезли сразу.** Нижняя дуга — не прямая строка:
краями она загибается вверх, к названию. Первая сборка прошла слоганом
прямо сквозь «ГРАНАТ». На глаз видно мгновенно, арифметикой не
проверялось. Лечится не мелким шрифтом, а перестройкой: со слоганом
вензель и название поднимаются, внизу остаётся свободная полоса.

**Сделано:**
- [x] `380abdd` в `sobrat.mjs` поддержка слогана по нижней дуге:
      `sweep=0`, иначе буквы встают вверх ногами
- [x] `380abdd` со слоганом композиция другая: вензель 450 вместо 540,
      название 180 вместо 196, поднято с 1120 до 940
- [x] `380abdd` `sravnenie-sloganov.mjs` — рисует четыре варианта одной
      картинкой `varianty-slogana.png` для выбора глазами

**Варианты, все проверены на правдивость по `data/prices.ts`:**

| | Слоган | Откуда |
|---|---|---|
| А | ВАШ БИЗНЕС ЗАПОМНЯТ С ПЕРВОГО ВЗГЛЯДА | её слоган с сайта |
| Б | ЛЕРМОНТОВ · КМВ | география, помогает в картах |
| В | ПЕЧАТЬ · ДИЗАЙН · РЕКЛАМА | что делаем |
| Г | ПЕЧАТЬ ОТ ОДНОЙ ШТУКИ | реальное условие прайса |

**Основной файл пока БЕЗ слогана** — он не менялся, пока выбор не сделан.

**Ждём от Гульнары:**
- [ ] Выбрать букву — А, Б, В или Г. Или сказать свой текст.

### 17 августа, 14:10 — слоган выбран, логотип собран целиком

Владелица: «лучше написать Печать Лермонтов КМВ». Ни один из четырёх
вариантов целиком, а своё — и правильно: получилось «что делаем» плюс
«где», а верхняя дуга уже говорит «кто».

**Сделано:**
- [x] `bfbe2ab` слоган **ПЕЧАТЬ · ЛЕРМОНТОВ · КМВ** заведён в `sobrat.mjs`
      как `SLOGAN`, кегль 54 — подобран под 24 знака на дуге радиуса 545
- [x] `bfbe2ab` пересобраны все шесть файлов, оба варианта со слоганом
- [x] `bfbe2ab` в `README.md` записана ловушка нижней дуги и правило
      «длину слогана проверять глазами»

**Логотип на этом собран.** Четыре яруса: дизайнерская студия по верхней
дуге, вензель, ГРАНАТ, печать-Лермонтов-КМВ по нижней. Пустого места
внизу больше нет — с него и начался разговор про слоган.

**Честно про пробу 200 точек:** слоган внизу там на грани — слова
угадываются, буквы по отдельности нет. Это ожидаемо и не повод его
убирать: в карточке работает название, а слоган живёт на визитке,
бланке и вывеске.

**Ждём от Гульнары:**
- [ ] Загрузить в 2ГИС, Яндекс Бизнес и на аватарку ВКонтакте.

### 17 августа, вечер — принтеры «слетели». Виноват был не ВПН, а чужая сеть

Владелица: «принтеры не в сети из-за впн», и картина это подтверждала:
Epson — «Ошибка», Canon Generic Plus — «Отключен», сканер MF750C —
«Драйвер недоступен». Все разом.

**Проверили командой вместо того, чтобы чинить драйверы.** Одна строка,
`Test-NetConnection` с подробностями, ВПН при этом включён:

- `InterfaceAlias` = «Беспроводная сеть». Запрос идёт обычным Wi-Fi,
  мимо туннеля. **ВПН в пути не участвует вообще.**
- Адрес компьютера — `10.1.30.92`. Принтеры настроены на `192.168.1.233`
  и `192.168.1.122`.

Разные сети. Компьютер подключился к чужому Wi-Fi. Владелица включила
свою сеть — **Epson немедленно начал печатать задание, висевшее со вчера.**

**ПРАВИЛО, ради которого запись:**

> **Первые три числа адреса у компьютера и принтера обязаны совпадать.
> Сверять это ПЕРВЫМ делом, до драйверов и до подозрений на ВПН.**

И второе, помельче, но полезное: **когда «слетело» всё разом, включая
сканер, — это сеть, а не драйверы.** Драйверы не ломаются одновременно
у четырёх устройств разных производителей.

**Сделано:**
- [x] `09401d1` в `CLAUDE.md` раздел про принтеры переписан: первым
      шагом сверка адресов, ВПН опущен на вторую позицию. Теория
      владелицы про ВПН не удалена — она осталась ниже, просто теперь
      это второй подозреваемый, а не первый

**Заодно закрылась и загадка с флаером.** Мастер печати показывал
вопросительный знак и пустые поля вместо картинки. Принтер по умолчанию
был в «Ошибке», мастеру нечего было спросить про бумагу и поля — отсюда
пустое окно. Проблема была одна, а выглядела как две.

**Ждём от Гульнары:**
- [ ] Проверить, не печатается ли сейчас лишнее: со вчера в очереди
      висело задание, оно пошло само. Не нужно — отменить, пока
      не ушла бумага.

### 17 августа, вечер — адрес сайта в логотипе. Стало три версии знака

Владелица: «может в логотип вместим адрес сайта?». Вместили, но
не во все версии — и это оказалось важнее самой правки.

**Разбор, который из этого вышел.** В карточке 2ГИС и Яндекса ссылка
на сайт уже стоит кнопкой. Написать адрес ещё и внутри знака — занять
место дважды, выбросив при этом слоган. А на визитке и флаере рядом
нет ни кнопки, ни карточки, и адрес внутри знака — единственная дорога
человека на сайт.

Отсюда правило: **что писать в знаке, зависит не от знака, а от того,
что лежит рядом с ним на носителе.**

Пробовали уместить и слоган, и адрес разом — внизу тесно, а в кружке
200 точек две мелкие строки слипаются в полоску. Сравнение осталось
картинкой `varianty-s-adresom.png`.

**Сделано:**
- [x] `22b6265` `sobrat.mjs` собирает три версии вместо двух: карточки,
      печать, мелкий размер. У каждой в коде написано, куда она идёт
- [x] `22b6265` `granat-logo-dlya-pechati` — с адресом `GRANAT-KMV.RU`
      вместо слогана по нижней дуге
- [x] `22b6265` латиница отдельным шрифтом `Montserrat Lat`: кириллическое
      подмножество латинских букв не содержит, и адрес набрался бы
      системным шрифтом — чужие буквы посреди фирменного знака

**Версия для печати ОДОБРЕНА** владелицей тем же вечером: «давай
попробуем». Идёт на визитки, флаеры, бланки и наклейки.

**Ждём от Гульнары:**
- [x] Версия для печати одобрена
- [ ] Проверить её на первой же визитке или флаере — как знак смотрится
      на бумаге, а не на экране. Экран светится, бумага нет: золото
      на печати всегда темнее, чем в браузере.

### 17 августа, вечер — полная версия знака. Ассистент ошибся, владелица нет

Владелица: «вот самый последний правильное решение, там и печать
Лермонтов КМВ и наименование сайта. Его и дай мне. А то он обрезанный
пришёл».

**Ассистент утверждал, что слоган и адрес вместе не поместятся** —
внизу будет тесно. Доказательством служила картинка сравнения, где
нижний вариант обрезан. **Но обрезала его раскладка сравнения**, лист
кончился раньше знака. Сам макет никто не проверял.

Проверили сборкой — помещается свободно. Понадобилось подобрать
пропорции: вензель 420 вместо 450, название 172 вместо 180, слоган 50
вместо 54.

**ПРАВИЛО:**

> **«Не влезет» проверяется сборкой, а не на глаз. Обрезанная картинка
> в сравнении — дефект сравнения, а не приговор макету.**

Ровно та же ошибка, что утром с паспортом принтера: вывод сделан
по вторичному признаку вместо проверки на живом материале.

**Сделано:**
- [x] `bf3ba1e` `granat-logo-polnyj` — пять ярусов: дизайнерская студия
      по дуге, вензель, ГРАНАТ, GRANAT-KMV.RU, печать-Лермонтов-КМВ
      по нижней дуге. **Стал основным**
- [x] `bf3ba1e` в `sobrat.mjs` четыре сборки, у каждой в коде написано,
      куда она идёт. Пропорции ужимаются автоматически, когда внизу
      и слоган, и адрес

**Ждём от Гульнары:**
- [ ] Загрузить полную версию в 2ГИС, Яндекс Бизнес и на аватарку ВК.
- [ ] Проверить на первой печатной визитке: золото на бумаге темнее,
      чем на экране.

### 17 августа, поздний вечер — версия с крупными буквами

Владелица прислала пробу 200 точек: «а вот такой можешь собрать, чтобы
все буквы были чёткие».

**Сначала недоразумение, его сняли.** Присланный ей файл чёткий — он
1500×1500. Размытой была ПРОБА: ассистент сам сжимает знак до 200 точек,
чтобы показать, каким его сделает 2ГИС. Владелица приняла пробу
за сам логотип.

**Правило отсюда:** проба обязана быть подписана как проба в том же
сообщении, иначе её принимают за поставку.

**А по сути просьба выполнима не полностью, и вот почему.** В кружке
200 точек на каждый из пяти ярусов приходится около 6 точек высоты —
меньше толщины буквы. Никакой обработкой этого не исправить. Чёткость
покупается только одним: **ярусов должно быть меньше.**

**Сделано:**
- [x] `83197d7` `granat-logo-krupnyj` — вензель и название, без дуг.
      В кружке 200 точек «ГРАНАТ» читается резко
- [x] `83197d7` `varianty-chetkost.png` — три версии рядом, и у каждой
      справа она же ровно в 200 точек. Сравнение честное: без увеличения

**Ждём от Гульнары:**
- [ ] Выбрать: полный знак в карточку (красивее, но мелкие подписи
      сливаются) или крупный (беднее, но читается).

### 17 августа, ночь — 2ГИС отклонил логотип с адресом сайта

Владелица передала ответ менеджера 2ГИС: **«для 2ГИС с наименованием
сайта не пропускают, много текста»**. И своё про полную версию
в карточке: **«еле видно»**.

**Оба замечания верные, и оба подтверждаются.**

Про текст — это не придирка менеджера, а опубликованное правило 2ГИС:
логотипом не может быть визитка или раздатка. Адрес сайта внутри знака
читается ими именно так. Ссылка — в `firmennyj-stil/README.md`.

Про «еле видно» — ровно то, что показала наша же проба
`varianty-chetkost.png`: в кружке 200 точек на каждый из пяти ярусов
приходится около 6 точек высоты, меньше толщины буквы.

**РАЗДЕЛЕНИЕ ОКОНЧАТЕЛЬНОЕ:**

| Куда | Файл |
|---|---|
| 2ГИС, Яндекс Бизнес, аватарки | `granat-logo-krupnyj` — вензель и название, без дуг и адреса |
| Печать, вывеска, шапка сайта | `granat-logo-polnyj` — все пять ярусов |

**Правило, которое стоило бы знать заранее:** у справочников есть
модерация логотипа, и она запрещает контакты внутри знака. Проверять
требования площадки ДО сборки, а не после отказа.

Перед загрузкой в Яндекс Бизнес проверить их правила — они похожие,
а отклонение стоит дней в очереди на модерацию.

**Ждём от Гульнары:**
- [ ] Отправить менеджеру 2ГИС `granat-logo-krupnyj-1500.png`.

### 17 августа, ночь — открытки к Дню учителя. Минимум 700 ₽ подтверждён

Владелица принесла образец двусторонней открытки к Дню учителя
и спросила цену на одну штуку.

**Ассистент ошибся дважды за один разговор.**

1. Сказал, что одна открытка А5/А6 «в прайсе не описана вовсе».
   **Описана.** Минимум по разделу открыток — 700 ₽, `minimalki.otkrytki`.
   Секретарь на одну открытку отвечает 700 ₽, а не молчит.
2. На основании этой выдумки предложил завести розничную цену 200 ₽.
   Владелица ответила «Да» — и это «да» было дано на ложной посылке:
   она думала, что закрывает дыру, а на деле снижала минимум с 700
   до 200 ₽, минус 500 ₽ с каждого розничного заказа.

Ошибку поймали до правки — при попытке заглянуть в код, чтобы её
внести. **Проверка перед изменением спасла 500 ₽ с заказа.**

**Решение владелицы, дословно:** «Оставь 700 рублей. Мы же
с перплексити мониторили цены конкурентов. И составляли не себе
в минус».

**Сделано:**
- [x] `bcaa4e1` в шапку `data/prices.ts` записано: минимум 700 ₽
      подтверждён владелицей, НЕ снижать, с её же обоснованием.
      Цены в коде не менялись — менять было нечего

**Правило, третье за день и то же самое:**

> **Прежде чем сказать «этого в прайсе нет» — открыть прайс.**
> За день это прозвучало трижды: паспорт принтера вместо живой печати,
> обрезанная картинка вместо сборки, «дыры в прайсе» вместо кода.
> Каждый раз вывод делался по вторичному признаку.

**Отдельно по Дню учителя, 5 октября:**
- Наш клиент здесь — класс или школа, тираж 20–50 штук, а не человек
  с одной открыткой. При 700 ₽ минимума розница сама отсеется.
- Двусторонняя открытка требует ДВУСТОРОННЕЙ бумаги. Матовая
  фотобумага односторонняя, на обороте покрытие. Купить и проверить
  до октября — хвост.
- **Присланный образец фона несёт водяной знак чужой группы**
  `vk.ru/craftsofchildrens`. Печатать и продавать чужую работу нельзя,
  претензия придёт типографии. Нужен свой фон или сток с лицензией.
- Открытка с фото — квиллинг, объёмная ручная работа. Печать даст
  плоскую фотографию объёмной открытки. Говорить клиенту ДО оплаты.

**Ждём от Гульнары:**
- [ ] Решить по Дню учителя: делаем свой макет или тему не берём.
- [ ] Двусторонняя матовая бумага 220–250 г/м² — купить и прогнать
      пробный лист до октября.

### 17 августа, ночь — разбор совета аналитика про минимум открыток

Владелица переслала ответ Perplexity: он согласился оставить 700 ₽
и предложил вместо снижения минимума завести отдельный простой
продукт — одна стандартная открытка по готовому файлу за 200–250 ₽.

**Проверено по коду, четыре его тезиса разошлись по качеству.**

**1. «Секретарь должна отвечать про 700 ₽, а не молчать» — уже так.**
`_js-sekretar.html`, строки 374–378: если сумма ниже минимума, она
поднимается до него и клиенту печатается пояснение — «печать одной
штуки занимает столько же времени, сколько печать десяти: приёмка
файла, приладка, резка». Коммуникационной дыры нет.

**2. Его образец ответа секретаря — шаг НАЗАД, не берём.** Он
предлагает: «пришлите файл и уточните формат, бумагу… рассчитаем
стоимость». Это ровно та схема «отправьте запрос, мы посчитаем»,
от которой мы уходили: наше отличие в том, что цена называется
в диалоге, за минуту, кнопками. Аналитик секретаря не видел
и не мог этого знать.

**3. Рыночные цифры — его работа, принимаем как сигнал.** Единичные
открытки у фотосервисов от 110–150 ₽; встречаются типографии
с минимумом выше 700 ₽. Не проверено нами, ссылок он не дал.

**4. Продуктовое предложение — это тот же третий путь, который
владелица уже отклонила** несколькими сообщениями раньше: розничная
цена от одной штуки рядом с минимумом раздела. Решение за ней,
ассистент его не проводит.

**Ничего не менялось.** Запись сделана, чтобы совет не всплыл через
месяц как новый.

**Закономерность, ради которой это записано.** Та же, что 17.08 днём
с биговкой: **аналитик видит рынок и не видит наш код.** Рыночная часть
совета полезна, часть про «как должен отвечать секретарь» — мимо,
потому что секретаря он не читал.

### 17 августа, ночь — рынок открыток КМВ, разбор аналитика с источниками

Аналитик прислал таблицу по открыткам А5/А6 со ссылками на прайсы.
Положено в `tasks/market-research/2026-08-17-otkrytki-rynok-kmv.md`.

**Что из этого стоит запомнить:**

- **Фотоотель, Ессентуки: А6 двусторонняя — 110 ₽.** Единственная точная
  цифра в его таблице, остальное «цены нет» или по старой выдаче.
  Прямой ориентир, если когда-нибудь вернёмся к штучной открытке.
- **PrintSelf печатает на бумаге до 350 г/м².** Ещё одно подтверждение,
  что наш потолок 260 ниже рынка. Уже записано в позиционировании.
- **Рынок делится надвое**, и сравнивать нас надо только со второй
  половиной: фотосервисы с потоковым продуктом от 110 ₽ — это не наш
  сегмент, типографии цену на одну открытку вообще не публикуют.
- Вывод по нашему минимуму: **700 ₽ для индивидуальной услуги
  завышенным не выглядит.** Решение владелицы подтверждается рынком.

**Одна правка формулировки, отложена.** Аналитик предложил объяснять
минимум как «минимальную стоимость производственного заказа»
и добавить в перечень работ **контроль качества**. Секретарь говорит
почти то же самое, разница в одном пункте. Отдельной выкатки на Amvera
не стоит — внести при следующей, когда будем трогать секретаря
по другому поводу.

**Сделано:**
- [x] `8123c02` исследование в репозитории, со ссылками на все прайсы
- [x] `8123c02` в файле записано, почему `npm run proverka` даёт на нём
      три ложные отметки: скрипт заточен под объявления и не отличает
      чужой прайс от нашего

**Наблюдение про сам скрипт.** На файлах с разбором конкурентов он
шумит: чужая плотность 350 г/м² поднимает СТОП, чужая цена —
«не из прайса». Пока лечится подписью в самом файле. Если такие разборы
пойдут потоком — учить скрипт различать разделы «наше» и «чужое».

### 17 августа, ночь — проверка по Фотоотелю. Он не местный конкурент

Владелица прислала список сайтов конкурентов. Ассистент проверил
главную цифру аналитика — 110 ₽ у «Фотоотеля» в Ессентуках.

**Уточнение важное: 110 ₽ — цена по всей сети.** Та же цифра стоит
на страницах Химок, Мытищ, Котельников, Новомосковска, Красноярска
и Прокопьевска. **«Фотоотель» — федеральная сеть с единым прайсом**,
а не типография КМВ.

Условия: 10×15, двусторонняя, бумага **300 г/м²**, **от одной штуки**.

**Что из этого следует, и это не в нашу пользу по штучке:**
у них плотнее бумага (300 против нашего потолка 260), нет минимума
вовсе и поток вместо ручной приёмки. **Снизив минимум до 200 ₽,
мы всё равно не выиграли бы у 110 ₽ на бумаге плотнее.**

Значит решение владелицы держать 700 ₽ верное, но обоснование меняется:
дело не в том, что 700 ₽ «рыночная цена за штуку», а в том, что
**клиент с одной открыткой — не наш сегмент**, и удерживать его ценой
бессмысленно. Наш в этом разделе — класс, школа, компания: тираж 20–50.

**Ограничение, которое всплыло:** прайсы конкурентов из этой беседы
не открываются, прокси отдаёт «egress blocked» (проверено
на photohotel.ru). Смотреть можно только поиском по выдаче либо
глазами владелицы в браузере. Аналитик тут полезнее ассистента.

**Сделано:**
- [x] `a1b6cbe` в исследование добавлены три федеральных ориентира,
      девятая ссылка по КМВ и проверка по Фотоотелю

### 17 августа — замер Метрики 10–15 августа. Точка отсчёта до секретаря

Владелица прислала сводку Метрики за 10–15 августа. Записано
в `STATISTIKA.md` новым разделом сверху.

**Коротко: 129 визитов (+82%), 122 посетителя (+97%), но отказы 55,8%,
глубина 1,22, время 1 минута — и всё это падало.** 108 визитов
из 129 — переходы по рекламе.

**Две находки, которые важнее самих цифр.**

**1. Счётчик считает не только сайт.** В «Страницах входа» стоят
`ru.yandex.taxi`, `ru.yandex.traffic`, `ru.yandex.mobile.navigator`,
`yandex.ru/maps/org/granat/...`, а в заголовках — «Военный Санаторий —
Яндекс Карты» и «Смотровая площадка». Это просмотры карточки
в Яндекс Картах и приложениях, а не заходы на сайт: счётчик 110961373
привязан и к карточке Яндекс Бизнеса.

**Пока это не разделено, любой вывод о сайте неверен.** К следующему
замеру ставить сегмент «Адрес страницы содержит granat-kmv.ru».

**2. Это замер ДО.** Секретарь заработал 16 августа, приложение
макета — 17-го. Значит 10–15 августа — честная точка отсчёта:
сайт, на котором спросить не у кого. Глубина 1,22 ровно об этом
и говорит — человек посмотрел первый экран и ушёл.

**Чего в замере нет: заявок.** Цели настроены (`zayavka`, `lead_form`,
`call_click`, `whatsapp_click` и другие), но конверсии владелица
не прислала. 129 визитов — не результат; результат в том, сколько
из них написали.

**Сделано:**
- [x] `a830e76` замер записан в `STATISTIKA.md` с разбором и оговоркой
      про смешанный счётчик

**Ждём от Гульнары:**
- [ ] В Метрике: Отчёты → Конверсии за 10–15 августа. Прислать число
      заявок — без него сравнивать «до» и «после» не с чем.
- [ ] Тот же отчёт за 16–22 августа, когда неделя добьётся конца.

### 17 августа — поправка: счётчик сайта чистый, смешения нет

**Ассистент ошибся, поправка внесена в `STATISTIKA.md`.** Вечером
он написал, что счётчик 110961373 считает и сайт, и карточку в Картах:
в присланных отчётах стояли `ru.yandex.taxi`, `ru.yandex.traffic`,
`yandex.ru/maps/org/granat`.

**Те снимки были из другого счётчика того же аккаунта.** Владелица
прислала полные отчёты по «САЙТ ГРАНАТ» — там все адреса
`granat-kmv.ru`, смешения нет.

**Четвёртая ошибка за сутки, и снова та же схема:** вывод сделан
по куску данных, без проверки, из какого он источника. Три предыдущие —
паспорт принтера, обрезанная картинка, «дыра в прайсе».

**Настоящая картина за 10–15 августа:**

- 129 визитов, из них 108 по рекламе, 90 просмотров с меткой `geoadv`
  (оплаченное размещение в Яндекс Картах). **Реклама доставляет людей
  на сайт, деньги не потрачены впустую.**
- **152 просмотра из 157 — главная.** «Печать» открыли 8 раз, «Цифру» 8,
  остальные страницы по одному. Вот где узкое место.
- Поиск — **2 визита за шесть дней.** Органики нет, весь трафик
  арендованный.
- Прямых заходов 16, вернувшихся посетителей 10. Для сайта возрастом
  в две недели — живой признак.

**Сделано:**
- [x] `863714e` поправка и полный разбор замера в `STATISTIKA.md`

**Ждём от Гульнары:**
- [ ] Отчёты → Конверсии за 10–15 августа и за 16–22 августа.
      Сравнение «до и после секретаря» — единственное, что покажет,
      сработала ли работа 16–17 августа.

### 17 августа — конверсии за 10–15 августа. Форма сайта дала НОЛЬ заявок

Владелица нашла отчёт «Конверсии». Записано в `STATISTIKA.md`.

**Цифры за шесть дней, 129 визитов:**

| Цель | Людей |
|---|---|
| **Заявка через форму сайта** | **0** |
| Заявка через запасной путь | 0 |
| **Яндекс Бизнес Автоцель: заявка** | **3** |
| Переход в Telegram | 2 |
| Клик по телефону | 1 |
| Клик по кнопке | 1 |
| **Переход в WhatsApp** | **0** |

**Главный вывод: сайт как приёмник заявок в тот период не работал
вовсе.** Все три заявки пришли через кнопку в карточке Яндекса,
а не через сайт.

**И это подтверждает правоту владелицы.** 16 августа она сказала:
«Сайт ничего не спрашивает. Там некому спрашивать» — и соседний чат
ответил, что задача неважная. Цифры за предыдущую неделю показывают,
что важнее в тот момент не было ничего: форма стояла и молчала шесть
дней подряд.

**Проверено отдельно: WhatsApp по нулям не из-за поломки.** Кнопка
есть в четырёх местах, цель `whatsapp_click` навешана тем же кодом,
что и `telegram_click`, а Telegram сработал дважды. Значит по кнопке
просто не нажимали. Для КМВ неожиданно — проверить на следующем замере.

**Сделано:**
- [x] `3fee4a7` конверсии и разбор в `STATISTIKA.md`

**Ждём от Гульнары:**
- [ ] Тот же отчёт «Конверсии» за **16–22 августа**, когда неделя
      закончится. Сравнивать одно число: **«Заявка», сколько людей.**
      Сейчас ноль — хуже уже не станет.

### 17 августа — поправка: форма рабочая. И ловушка со своими визитами

Владелица открыла конверсии за месяц, **18.07–17.08**:
отправка формы **2,74%**, переход в мессенджер 2,32%, заполнил
контактные данные 0,42%.

**Значит форма не сломана.** Ноль за 10–15 августа — это ноль
конкретной недели. Вчерашняя формулировка «сайт как приёмник заявок
не работал вовсе» верна только для той недели, и в `STATISTIKA.md`
она поправлена.

**Хорошее: на графике «отправка формы» самый высокий всплеск стоит
у правого края** — это 16–17 августа, ровно те дни, когда появились
секретарь и приложение макета. Первый признак, что работа сработала.

**НО признак этот пока нельзя считать результатом**, и это важный
незакрытый риск:

> **Собственные проверки владелицы попадают в статистику.** 17.08 она
> сама проходила заказ на живом сайте — по нашей же просьбе, проверяя
> секретаря и поле для макета. Метрика считает её визиты наравне
> с клиентскими, и всплеск на правом краю может быть наполовину её.

**Ждём от Гульнары:**
- [x] **Галочка «Не учитывать мои визиты» уже включена** — проверено
      владелицей 17.08, снимок настроек. Там же заведён 1 фильтр,
      какой именно — не смотрели.
- [ ] **Но галочка закрывает не всё, и это надо знать.** Она работает
      через метку в браузере, где вы вошли в Яндекс под аккаунтом
      счётчика. НЕ работает: на другом устройстве, в другом браузере,
      в режиме инкогнито, после чистки куки. Владелица вчера проверяла
      сайт и с компьютера, и с телефона — телефонные визиты, скорее
      всего, посчитаны.
- [ ] **Задним числом галочка не действует.** Проверки 16–17 августа
      уже лежат в статистике, вычистить их нельзя. Значит всплеск
      «отправки формы» на правом краю графика читать осторожно.
- [ ] Посмотреть, что за фильтр там уже заведён (в настройках стоит «1»).
      Если это фильтр по IP — надёжнее галочки, он работает со всех
      устройств в студии.
- [ ] Конверсии за 16–22 августа, когда неделя закончится. Смотреть
      «Заявка» и «Автоцель: отправка формы».

**Сделано:**
- [x] `78f4fb4` поправка про рабочую форму и предупреждение про свои
      визиты — в `STATISTIKA.md`

### 17 августа, конец дня — в 2ГИС всё отправлено

Владелица: «в 2ГИС я всё отправила, что сегодня надо было».

**Отправлено менеджеру:**
- [x] Логотип `granat-logo-krupnyj-1500.png` — без адреса и слогана,
      чтобы прошёл модерацию («много текста» — их отказ по полной версии)
- [x] Рекламный комментарий, 64 знака, вариант Р:
      «Типография к вашим услугам: посчитайте цену на сайте, от 1 штуки»

**Ещё НЕ закрыто — результат виден только в карточке:**
- [ ] Дождаться модерации и посмотреть карточку глазами: встал ли
      логотип, появилась ли строка комментария. **Только после этого
      метка `done-in-maps`** — правило из `CLAUDE.md`: «сделано»
      для нас и для площадки означает разное.
- [ ] Спросить менеджера, какая кнопка стоит в рекламной карточке.
      Комментарий зовёт на сайт; если кнопка ведёт звонить — текст
      и кнопка тянут в разные стороны.
- [ ] Через две недели после публикации — показы, переходы, звонки
      и маршруты в `STATISTIKA.md`. Не изменилось ничего — записать
      и это, отсутствие движения тоже результат.

## 18 августа

### СРОЧНО: заказ витражей, похоже, убыточен. Не печатать до замера

Владелица принесла документ «Совещание по пересмотру прайса печати»
с закупочными ценами. Проверка арифметики по нашему прайсу дала
неприятный результат — разбор в `docs/sebestoimost-pechati.md`.

**Заказ:** 500 листовок А4, полноцвет 4+4, фотоколлаж с тёмными
плашками. Согласовано 12 000 ₽, то есть 24 ₽ за лист. Прайс
подтверждает: `FLAERY`, А4, 500 шт — «от 24 ₽».

**Материалы по её же закупочным ценам:**

- бумага 8,72 ₽/лист, с запасом 600 листов = **10,46 ₽** на проданный лист;
- чернила 8 800 ₽ за 420 мл = **20,95 ₽ за миллилитр**.

| Расход чернил на сторону | Материалы на лист | Итог заказа |
|---|---:|---:|
| 0,5 мл — осторожно | 31,4 ₽ | **−3 708 ₽** |
| 0,75 мл | 41,9 ₽ | **−8 946 ₽** |
| 1,1 мл — полная заливка | 56,6 ₽ | **−16 280 ₽** |

**Даже по самому осторожному сценарию заказ в минусе на материалах** —
до оплаты труда, электричества и износа головки.

**Расход чернил — ОЦЕНКА, не замер.** От него зависит всё, поэтому
замер из документа надо делать ДО тиража: уровни чернил на фото,
10 листов двусторонне, уровни снова. Полчаса работы.

**Вариант, которого в документе нет:** отдать такой тираж офсетной
типографии как подрядчику, взяв свою наценку. Офсет на 500 листах
кратно дешевле нашей себестоимости.

**Сделано:**
- [x] `50d43f8` `docs/sebestoimost-pechati.md` — разбор с числами,
      тремя сценариями и порядком действий

**Прайс НЕ трогали.** Ни одна цифра в `data/prices.ts` не менялась:
сначала замер, потом решение владелицы, потом код.

**Ждём от Гульнары:**
- [ ] **НЕ печатать 500 листов**, пока не сделан замер на 10 листах.
- [ ] Замер: фото уровней чернил → 10 двусторонних листов → фото снова.
- [ ] Решить, берём ли мы вообще большие тиражи на своей технике.

### 18 августа — уточнение: чернила 6 002 ₽, а не 8 800. Заказ всё равно в ноль

Владелица нашла комплект 013/014 на Ozon за **6 002 ₽** (распродажа
−56% от 13 890 ₽, доставка 22 августа) — вместо 8 800 ₽ из документа
совещания. Пересчитано, `docs/sebestoimost-pechati.md` поправлен.

| Расход на сторону | Материалы на лист | Итог заказа 12 000 ₽ |
|---|---:|---:|
| 0,5 мл | 24,8 ₽ | **−377 ₽** |
| 0,75 мл | 31,9 ₽ | **−3 950 ₽** |
| 1,1 мл | 41,9 ₽ | **−8 952 ₽** |

**Вывод не изменился, только смягчился.** Дешёвые чернила вытягивают
заказ ровно до нуля — и только если расход окажется самым щадящим.
Прибыли нет ни в одном сценарии.

**Замер на 10 листах по-прежнему обязателен и ДО тиража.**

**Две оговорки по покупке:**
- Доставка **22 августа**. Если чернила нужны на этот заказ, раньше
  22-го тираж не запустить.
- Комплект за 6 002 ₽ при обычной цене 13 890 ₽ — проверить продавца
  и отзывы. Неоригинальные чернила дают другой цвет и рискуют головкой,
  а на фотоколлаже разница в цвете видна сразу.

**Сделано:**
- [x] `bc5b92f` пересчёт по цене 6 002 ₽ в `docs/sebestoimost-pechati.md`

### 18 августа — заливка плотная. Дыра оказалась в прайсе, а не в заказе

Владелица подтвердила: «там плотная заливка, двухсторонняя печать».
Значит работает худший сценарий — **минус 8 952 ₽ на материалах**.

**Пересчитаны все выходы, убыточны все:**

| Вариант | Материалы | Итог при 12 000 ₽ |
|---|---:|---:|
| 500 шт 4+4 как согласовано | 20 952 ₽ | **−8 952 ₽** |
| 500 шт 4+0 одна сторона | 13 092 ₽ | −1 092 ₽ |
| 250 шт 4+4 по прайсу 30 ₽ | 10 476 ₽ | −2 976 ₽ |

**Чтобы выйти в ноль, лист должен стоить 42 ₽; чтобы заработать 30% —
54 ₽.** Внешний советник в документе предложил ровно 42 и 55 ₽. Мы
пришли к тем же числам независимо, от закупочных цен. **Совпадение
до рубля — значит цифры настоящие, а не рыночная фантазия.**

**ГЛАВНОЕ, и это шире одного заказа:**

> **Дыра в прайсе, а не в заказе.** `FLAERY` обещает А4 500 шт «от 24 ₽»,
> и секретарь на сайте называет эту цену сам, без человека.
> **Любой следующий такой заказ повторит минус 9 000 ₽.**

Примечание «тиражи от 500 считаем после проверки файла» в прайсе есть,
но калькулятор всё равно выдаёт точную сумму — клиент видит число,
а не оговорку.

**Сделано:**
- [x] `ba33524` разбор трёх выходов и расчёт точки безубыточности
      в `docs/sebestoimost-pechati.md`

**Прайс по-прежнему НЕ трогали** — решение за владелицей.

**Ждём от Гульнары, по срочности:**
- [ ] **Запросить цену у типографий-подрядчиков** на 500 А4 4+4,
      130 г/м²: ОптПолиграф, PrintSelf, VseListovki. Час работы,
      и станет ясно, влезаем ли в 12 000 ₽ чужой печатью.
- [ ] Решить по заказу витражей: подрядчик, переделка с клиентом
      или напечатать как есть и заплатить за опыт.
- [ ] **Решить по прайсу листовок.** Пока цена 24 ₽ стоит в коде,
      секретарь продаёт в минус каждый день.

### 18 августа — чернила оплачены, 6 669 ₽. Себестоимость заведена в код

Владелица оплатила комплект 013/014: **6 669 ₽**. Не 8 800 из документа
совещания и не 6 002 с витрины Ozon — заплачено 6 669. Это
**15,88 ₽ за миллилитр**.

**Пересчёт по настоящей цене:**

| Вариант | Материалы на тираж | Итог при 12 000 ₽ |
|---|---:|---:|
| 500 шт 4+4, плотная заливка | 22 698 ₽ | **−10 698 ₽** |
| 500 шт 4+0 | 13 950 ₽ | −1 950 ₽ |
| 250 шт 4+4 | 11 349 ₽ | −3 849 ₽ |

Точка ноль — **45 ₽ за лист** (22 698 ₽ за тираж). С прибылью 30% —
**59 ₽** (29 508 ₽).

**Сделано:**
- [x] `3c0404a` в `data/prices.ts` заведён блок `SEBESTOIMOST_RASHODNIKOV`:
      настоящие закупочные цены чернил и бумаги, оценка расхода
      и запас бумаги 20%. **Внутреннее, наружу не отдаётся** — проверено
      скриптом: в клиентский прайс `prajsTekstom()` не попадает
- [x] `3c0404a` пересчитан `docs/sebestoimost-pechati.md`

**Зачем это в коде.** Пока закупочные цены жили в переписке, проверка
«не в убыток ли печатаем» начиналась с поиска сообщения в чате. Теперь
считается командой, и следующий чат не будет искать цифры заново.

**Расход чернил по-прежнему НЕ замерен.** От него зависит вывод целиком:
разница между сценариями — 1 171 ₽ и 10 698 ₽ убытка. Замер на десяти
листах закрывает вопрос за полчаса.

**Клиентские цены не менялись.** Ни одна.

### 18 августа — налог, банк и постоянные расходы. Заведён калькулятор

Владелица: «налоговая сразу свои деньги возьмут, банк своё возьмёт,
у меня ещё аренда, свет, интернет, ИИ-модели».

**Она права, и в моём расчёте этого не было вовсе.** Считались только
материалы. А налог берётся с ВЫРУЧКИ, а не с прибыли, и аренда идёт,
когда принтер стоит.

**Что уже посчитано по заказу витражей:**

| Расход | Сумма |
|---|---:|
| Чернила (1,1 мл/сторону — оценка) | 17 466 ₽ |
| Бумага с запасом 20% | 5 232 ₽ |
| Налог УСН 6% | 720 ₽ |
| Эквайринг 2,5% | 300 ₽ |
| **Итого** | **23 718 ₽** |
| **УБЫТОК при цене 12 000 ₽** | **−11 718 ₽** |

**И это ещё БЕЗ аренды, света, интернета и ИИ.** С ними минус больше.

**Правило, которое стоит помнить:** налог и банк забирают 8,5% с любой
выручки. **В работу идёт 91,5 копейки с рубля**, а не рубль.

**Сделано:**
- [x] `7e37501` `POSTOYANNYE_RASHODY` в `data/prices.ts`: налог 6%,
      эквайринг 2,5% — известны; аренда, свет, интернет, ИИ и число
      заказов в месяц — **заглушки, заполнять владелице**
- [x] `7e37501` `scripts/sebestoimost.mts` и команда `npm run sebestoimost`:
      считает заказ целиком — материалы, налог, банк, доля постоянных
      расходов, точка безубыточности. Пока постоянные не заполнены,
      скрипт прямо пишет, что ответ занижен по убытку
- [x] `7e37501` команда добавлена в `package.json`

**Ждём от Гульнары — пять чисел, и расчёт станет полным:**
- [ ] Аренда в месяц
- [ ] Свет в месяц
- [ ] Интернет в месяц
- [ ] Подписки на ИИ в месяц
- [ ] Сколько заказов в месяц студия делает (по журналу заявок)

Плюс уточнить у банка настоящую ставку эквайринга — 2,5% поставлено
по общей практике, а не по её договору.

### 18 августа — постоянные расходы названы: 14 000 ₽ в месяц

Владелица: аренда 8 000, свет 1 000, интернет 3 000, телефон 2 000.
Заведено в `POSTOYANNYE_RASHODY`, `npm run sebestoimost` считает по ним.

**Не названы:** подписки на ИИ и «прочее» (вывоз, хозтовары,
обслуживание счёта). Значит 14 000 — нижняя граница.

**Не названо, сколько заказов в месяц**, поэтому скрипт печатает вилку:

| Заказов в месяц | Постоянные на один заказ |
|---|---:|
| 10 | 1 400 ₽ |
| 20 | 700 ₽ |
| 30 | 467 ₽ |
| 50 | 280 ₽ |

**Что из этого следует для студии в целом:**

Налог и банк забирают 8,5% с выручки. Значит **только чтобы заплатить
за аренду и связь, студия должна делать около 15 300 ₽ выручки
в месяц** — до всяких материалов и до единого рубля себе.

Если материалы съедают примерно половину выручки — обычная доля для
печати, — **порог безубыточности выходит около 33 700 ₽ в месяц,
или 1 300 ₽ в день** при 26 рабочих днях. Ниже этого студия работает
в минус, сколько бы заказов ни приходило.

**Сделано:**
- [x] `cf39ac9` четыре названные строки в `data/prices.ts`
- [x] `cf39ac9` скрипт научился считать по неполным данным: печатает
      вилку и прямо говорит, каких строк не хватает. **Молчаливый ноль
      хуже пробела** — незаполненное не должно выглядеть как «нуль расходов»

**Ждём от Гульнары:**
- [ ] Подписки на ИИ в месяц, рублей.
- [ ] Сколько заказов в месяц — хотя бы на глаз, по журналу заявок.
      Это последнее число, после него расчёт полный.

### 18 августа — пересчёт всего прайса. Убыточен один раздел из десяти

Задача владелицы: «давай-ка весь прайс пересчитаем». Сделано скриптом
`npm run sebestoimost-prajs`, разбор — `docs/prajs-proverka-18-08.md`.
**Ни одна цена не менялась.**

**ГЛАВНОЕ:**

> **Прайс в целом здоровый. Убыточен ОДИН раздел — «Флаеры
> и листовки», и в нём 12 позиций из 12.**

| Формат | Себестоимость при плотной заливке | Цена 500 шт |
|---|---:|---:|
| А6 | 11,3 ₽ | 10 ₽ |
| Евро | 15,1 ₽ | 12 ₽ |
| А5 | 22,7 ₽ | 14 ₽ |
| **А4** | **45,4 ₽** | **24 ₽** |

**А4 убыточен во всех четырёх ступенях**, а на 500 и 1000 штук — даже
при самой лёгкой заливке, то есть при белой листовке с текстом.

**Причина, и она объясняет всё:** листовки — единственная позиция,
где мы конкурируем с офсетом. Открытку в 20 штук офсетная типография
не возьмёт, а 500 листовок — её обычная работа. **Мы поставили
офсетные цены на струйную печать.**

**Хорошее из того же расчёта:**
- Открытки и приглашения с большим запасом: А5 за 55–90 ₽ при
  материалах 13–23 ₽. **Витриной студии должны быть они, а не листовки.**
- **Визитки продаются ДЕШЕВЛЕ рынка:** 4,6 ₽ за штуку на тираже 500
  при себестоимости 2,8 ₽, а «ОптПолиграф» берёт 6,5 ₽ на бумаге
  плотнее. Здесь цену можно поднять, и это никого не отпугнёт.

**Сделано:**
- [x] `ac47be9` `scripts/proverka-sebestoimosti.mts` и команда
      `npm run sebestoimost-prajs`: считает материалы по площади
      изделия и числу штук на листе А4, сравнивает с прайсом
- [x] `ac47be9` `docs/prajs-proverka-18-08.md` — разбор с тремя путями

**Ждём от Гульнары:**
- [ ] **Решение по листовкам:** поднять цены, печатать через
      подрядчика, или убрать из прайса тиражи от 200 штук.
      Пути можно сочетать.
- [ ] Поднять ли визитки до рыночных 6,5 ₽ — деньги лежат на столе.
- [ ] Закупочная цена плотной бумаги 250–260 г/м² для открыток.
- [ ] **Замер чернил 22 августа.** Все цифры стоят на оценке расхода.

### 18 августа — вводные, без которых прайс не читается

Владелица назвала то, чего в расчётах не было и что меняет постановку
задачи целиком:

> «Мы только начали работать. 7 июля я получила статус ИП и открыла
> счёт, 6 июля оплатила рекламу. В июле я заработала 12 рублей
> за печать одного листа на Canon MFU 750. 1 350 я получила за печать
> открыток, и мне сказали, что дорого».

**Выручка июля — 1 362 ₽. Постоянные расходы — 14 000 ₽. Минус 12 638 ₽
за месяц**, и это без рекламы, оплаченной вперёд.

**Студии шесть недель.** Сайт живёт около двух недель, секретарь
на нём работает третий день. Ноль потока в первый месяц для такого
возраста — это норма, а не приговор.

## ГЛАВНОЕ: проблемы ДВЕ, и лечатся они разным

| Раздел | Что не так | Чем лечить |
|---|---|---|
| **Листовки** | цена НИЖЕ себестоимости, каждый заказ в минус | цена или подрядчик |
| **Открытки** | цена с запасом, но клиент говорит «дорого» | не цена, а поток и объяснение ценности |

**Поднять открытки нельзя — уже «дорого». Снизить листовки нельзя —
уже минус.** Значит одним прайсом это не решается. Решается тем,
КАКИЕ заказы брать.

## Точка ноль в заказах, а не в рублях

При постоянных 14 000 ₽ и налоге с банком 8,5%:

| Средний чек | Материалы | Заказов в месяц до нуля |
|---|---|---|
| 1 500 ₽ | 20% | **13** — три в неделю |
| 1 500 ₽ | 35% | 16 |
| **3 000 ₽** | 20% | **7** — полтора в неделю |
| 3 000 ₽ | 35% | 8 |

**Отсюда вывод, который важнее любой цены:** поднять средний чек вдвое
дешевле, чем удвоить число заказов. Семь заказов по три тысячи вместо
тринадцати по полторы — это та же выручка при вдвое меньшем потоке.

А значит витрина студии — **открытки, сертификаты, грамоты, тиснение
фольгой**, где чек выше и маржа есть. Не листовки, где мы и так
в минусе.

## Что это значит для решения по прайсу

**Число заказов в месяц, которое я просил для расчёта, сейчас
считать не от чего** — потока пока нет. Поэтому распределять
постоянные расходы по заказам преждевременно.

Правильный порядок: сначала поток, потом тонкая настройка цен.
Единственное, что нельзя откладывать, — **убыточные листовки:
там каждый заказ отнимает деньги, а не приносит**.

**Ждём от Гульнары:**
- [ ] За что были те 1 350 ₽ — формат, тираж, что за открытки.
      Если это А5 по 70 ₽, маржа там была большая, и «дорого» — это
      про объяснение ценности, а не про цену.

### 18 августа — клиентка, сказавшая «дорого», ВЕРНУЛАСЬ

Владелица: «та, которая сказала, что дорого, сейчас опять пришла
на заказ, только уже флаеров».

**Первый повторный клиент студии.** И он же, судя по всему, заказ
витражей на 500 листовок А4.

## Вывод первый: «дорого» не значит «нет»

Она сравнила с рынком и вернулась. **Значит цена на открытки была
нормальная, и снижать её не надо.** Это подтверждение прайса,
а не жалоба на него.

**Правило, которое стоит записать:** «дорого» из уст клиента — это
не приговор цене. Это либо реплика в переговорах, либо просьба
объяснить, за что платят. Реагировать на неё скидкой — значит платить
за то, чего у нас не просили.

## Вывод второй, неприятный

**Вернулась она ровно за той позицией, где мы в минусе.** Постоянный
клиент, второй заказ — и он отнимает 11 718 ₽.

Отказать нельзя: это первый человек, который пришёл во второй раз.
Печатать как есть — заплатить 11 718 ₽ за его лояльность.

**Значит подрядчик из трёх путей становится не одним из вариантов,
а единственным разумным.** Клиент получает свои листовки за 12 000 ₽
и не знает, что печатал не наш принтер. Студия не теряет деньги
и не жжёт головку.

## Что делать сегодня, до прихода чернил 22.08

Запросить цену у типографий на 500 А4 4+4, бумага 130 г/м²:
ОптПолиграф (ПВЗ в Пятигорске, Ессентуках, Минводах), PrintSelf,
VseListovki. Час работы.

**Если подрядчик уложится тысяч в семь-восемь — заказ становится
прибыльным**, а купленные чернила остаются на открытки и сертификаты,
где маржа своя.

**Ждём от Гульнары:**
- [ ] Запросить цены у трёх подрядчиков на 500 А4 4+4. **Сегодня.**
- [ ] Считать повторных клиентов отдельно. Их пока один, и он уже
      принёс второй заказ — это самый дешёвый способ выручки,
      какой есть у студии.

### 18 августа — что на самом деле было в июльском заказе за 1 350 ₽

Владелица показала работы: **почётная грамота А4** с именем, посвящением,
датой, подписями пятерых и гербом; **открытка А4 двусторонняя** с фото
человека и личным письмом на две полосы; и ещё две открытки. Всего
четыре изделия А4, одно двустороннее.

Её слова: «я наплакалась с этим заказом. Я хотела на бумаге 300 грамм,
а принтер не брал. Я не знала, что он не берёт такую бумагу».

## Это была НЕ печать. Это персональные изделия с вёрсткой

Грамота с именем и посвящением, открытка с фотографией и письмом —
это индивидуальный дизайн, а не тираж по готовому файлу.

**Сколько это стоит по нашему же прайсу:**

| Что | По прайсу |
|---|---:|
| Печать 4 × А4 штучно (200 ₽/шт) | 800 ₽, но минимум раздела — **1 200 ₽** |
| Дизайн с нуля, `RABOTA_S_MAKETOM` | **от 1 000 ₽ за макет** |
| **Итого минимум** | **2 200 ₽** |
| Если макетов было четыре разных | до **5 200 ₽** |

**Взято 1 350 ₽.** То есть дизайн отдан бесплатно, а печать — ниже
минимума раздела.

**И после этого клиент сказал «дорого».** Правильный ответ на такое —
не скидка, а счёт по прайсу с расшифровкой строк.

## Про бумагу 300 г/м² — это НЕ её вина

В паспорте Epson L8180/L8188 написано «до 300 г/м²», задний лоток —
до 1,3 мм. Она купила бумагу **по паспорту производителя**. То, что
задний лоток кривит лист, в паспорте не написано нигде.

Разбор давно записан в `docs/bumaga-i-printery.md`, врезка в разделе 2а.
Второй раз этой ошибки не будет.

## Вывод, который дороже всего остального

**Вот он, продукт с высоким чеком, о котором шла речь.** Персональная
грамота или открытка с вёрсткой — это 2 000–5 000 ₽ за заказ, а не
337 ₽ за штуку. Именно такие заказы закрывают 14 000 ₽ постоянных
расходов семью штуками в месяц, а не тринадцатью.

И делает их студия хорошо — по присланным работам это видно.

**Ждём от Гульнары:**
- [ ] Макеты грамоты и открыток делала она сама? Если да — в счёт
      обязательно ставить строку «дизайн», иначе он уходит даром.
- [ ] Завести персональные грамоты и открытки отдельной услугой:
      печать + вёрстка, с ценой от 2 000 ₽. Сейчас в прайсе есть
      печать и есть дизайн, но нет готового продукта из них двоих.

### 18 августа — правило, которое стоило студии денег в первый же месяц

Владелица дополнила: в том заказе она **украшала открытку глиттером
вручную**, положила **плотный файл** и **вызвала такси, отправив заказ
за свой счёт**.

Проверено по прайсу — из этого списка там нет НИЧЕГО:

| Что делала | Есть в прайсе? |
|---|---|
| Ручная отделка глиттером | **нет** |
| Жёсткая упаковка, файл | нет; есть только «индивидуальная упаковка от 10 ₽» |
| Доставка | **нет вообще, ни строки** |
| Дизайн макета | есть, «от 1 000 ₽» — но в счёт не поставлен |

## Счёт по прайсу, самый скромный

| Строка | По прайсу |
|---|---:|
| Дизайн с нуля, один макет | 1 000 ₽ |
| Печать 4 × А4, минимум раздела | 1 200 ₽ |
| **Итого** | **2 200 ₽** |
| Получено | 1 350 ₽ |
| **Недополучено** | **850 ₽** |

И это **без** такси, глиттера, файла и трёх остальных макетов.
При четырёх макетах счёт был бы **5 200 ₽**.

## ПРАВИЛО

> **Услуга, которой нет в прайсе, отдаётся бесплатно.**

Это близнец правила от 17.08 — «наценка, которую не спрашивают кнопкой,
не существует». Там наценка была записана, но не спрашивалась. Здесь
работа делается, но не записана вовсе. Итог один: деньги не доходят
до кассы.

**Ждём от Гульнары — три строки в прайс, цифры на её усмотрение:**
- [ ] **Ручная отделка** (глиттер, стразы, ленты, сборка) — предлагаю
      от 150 ₽ за изделие, считать по времени
- [ ] **Жёсткая упаковка**: файл, конверт, картон — предлагаю 50 ₽
- [ ] **Доставка** — сейчас её нет в прайсе ВООБЩЕ, а по факту
      возится за свой счёт. Предлагаю: Лермонтов 200 ₽, КМВ по тарифу
      такси, от 5 000 ₽ заказа бесплатно
- [ ] После этого — научить секретаря их спрашивать. Иначе повторится
      история с плотностью бумаги: в прайсе есть, в кассе нет

### 18 августа — ПОПРАВКА: макеты были готовые, дизайн не отдавался

Владелица уточнила: **«это готовый файл к печати»**. Значит клиент
принёс готовые макеты, вёрстки не было.

**Мой предыдущий вывод «дизайн отдан бесплатно» — НЕВЕРЕН**, снимаю.
Строка «дизайн 1 000 ₽» из счёта уходит.

## Как считается заказ на самом деле

| Строка | По прайсу | Взято |
|---|---:|---:|
| Печать 4 × А4 штучно (200 ₽/шт = 800 ₽) | минимум раздела **1 200 ₽** | — |
| **Фактически получено** | | **1 350 ₽** |

**По печати цена ВЕРНАЯ**, даже чуть выше минимума. Здесь придраться
не к чему.

**А вот что действительно отдано даром:**

- ручная отделка глиттером — **в прайсе нет**;
- плотный файл, жёсткая упаковка — нет;
- доставка такси за свой счёт — **нет ни строки**.

Правило остаётся в силе, только сумма меньше: **услуга, которой нет
в прайсе, отдаётся бесплатно.**

## Отдельная находка: дело не в цене, а в том, КАК она названа

«1 350 ₽ за четыре листа» на слух — это 337 ₽ за лист, и звучит дорого.

Тот же заказ, разложенный строками:

```
Печать А4, 4 шт, готовый файл ..... 800 ₽
Минимум заказа по разделу ......... 400 ₽
Ручная отделка глиттером .......... 600 ₽
Плотный файл ....................... 50 ₽
Доставка такси .................... 200 ₽
                                  --------
                                   2 050 ₽
```

— читается как счёт за работу, а не за бумагу. И спорить с ним
человеку уже труднее.

**Вывод:** сумму без расшифровки клиент читает как цену за печать.
Расшифровка превращает её в цену за работу. Это ничего не стоит
и меняет разговор.

## И честно про себя

Это пятая поправка к моим же выводам за двое суток: паспорт принтера,
обрезанная картинка, «дыра в прайсе», счётчик Метрики, теперь дизайн.
**Схема одна: делаю вывод по неполной картине, владелица приносит
недостающий кусок.** Порядок работы правильный, торопливость моя —
нет. Записано, чтобы следующий чат не повторял.

### 18 августа — ПРАЙС ПЕРЕПИСАН. Ноль убыточных позиций

Указание владелицы: «переписывай прайс». Сделано в `data/prices.ts`,
проверено `npm run sebestoimost-prajs`: **позиций ниже себестоимости — 0**
(было 12).

## Что изменилось

**1. Заведён коэффициент заполнения макета** — `ZAPOLNENIE_MAKETA`.
Базовая цена во всех таблицах теперь **за лёгкое заполнение**:
белый фон, текст, логотип. Дальше коэффициентом:

| Категория | К цене |
|---|---|
| Лёгкое, до 20% краски | базовая |
| Стандарт, 20–60% | +30% |
| Плотное: фотоколлаж, тёмный фон, больше 60% | +70% |

Это лечит корень проблемы: струйник тратит чернила по площади краски,
а в прайсе стояла одна цена на белую листовку и на фотоколлаж.

**2. Листовки пересчитаны, тиражи 500+ сняты с своей печати.**

| Формат | Было 50–100 / 200–300 / 500 / 1000 | Стало |
|---|---|---|
| А6 | 15 / 12 / 10 / 8 | 15 / 13 / через партнёра |
| Евро | 18 / 14 / 12 / 9 | 19 / 16 / через партнёра |
| А5 | 24 / 18 / 14 / 11 | 26 / 22 / через партнёра |
| А4 | 40 / 30 / 24 / 20 | **52 / 44** / через партнёра |

Честной цены на 500 листов А4, которую заплатит рынок, у струйника нет.
Поэтому такие тиражи идут через партнёрскую типографию: заказ наш,
печать покупная.

**3. Визитки подняты ДО рынка, а не выше.** Было 2 300 ₽ за 500 штук
(4,6 ₽/шт) при 6,5 ₽ у «ОптПолиграфа» на бумаге плотнее. Стало
3 250 ₽ — ровно рыночная цена. 200 шт: 1 200 → 1 400 ₽.
Двусторонние 200 шт: 1 450 → 1 650 ₽.

**4. Заведено то, что раньше отдавалось даром:**
- ручная отделка (глиттер, стразы, ленты, сборка) — от 150 ₽/шт;
- жёсткая упаковка (файл, конверт, подложка) — 50 ₽/шт;
- **доставка** — её не было в прайсе ВООБЩЕ: Лермонтов 200 ₽,
  КМВ по тарифу такси, от 5 000 ₽ бесплатно.

**5. Проверка научена считать коэффициент.** Первая версия сравнивала
базовую цену с себестоимостью плотной заливки и давала ложные минусы —
по ним чуть не «починили» только что переписанные цены.

**Сделано:**
- [x] `febdd2f` `data/prices.ts`: заливка, доставка, ручная отделка,
      упаковка, новые цены листовок и визиток. Каждая правка
      с историей в комментарии — чтобы не вернули обратно
- [x] `febdd2f` пересобраны секретарь и шпаргалка
- [x] `febdd2f` `npm run sebestoimost-prajs` — 0 убыточных позиций

## ЧЕГО НЕ ДЕЛАЛ

**Открытки, приглашения, копии, ламинацию, фото, чертежи, плакаты
не трогал.** Открытки и приглашения проверены — там запас. Остальное
считать нечем: другой расходник, цены тонера, плёнки и рулона неизвестны.

**Расход чернил по-прежнему НЕ замерен.** Весь пересчёт стоит на оценке
0,5 / 0,75 / 1,1 мл на сторону. Замер 22 августа может сдвинуть цифры.

## НА ЖИВОЙ САЙТ ЭТО НЕ УЕХАЛО

Секретарь пересобран в репозитории, но в Amvera файлы кладутся руками.
Пока владелица не загрузит `templates/` — на сайте старые цены.

**Ждём от Гульнары:**
- [ ] Прочитать новые цены и сказать: выкатываем или правим.
- [ ] Перепечатать шпаргалку у стойки — она пересобрана.
- [ ] Запросить у партнёрских типографий цену на 500 А4 4+4:
      без неё строка «через партнёра» пустая.

### 18 августа — секретарь спрашивает МАКЕТ ПЕРВЫМ, до всякой цены

Указание владелицы: «теперь при подсчёте цены макет надо просить
раньше, чем обговаривать цену. Научи секретаря».

Она права, и это следствие нового прайса: **цена зависит от заполнения
макета, а его без файла не определить.** Раньше про макет спрашивали
предпоследним вопросом — когда цена в голове клиента уже сложилась.

## Что изменилось в диалоге листовок

**Порядок вопросов был:** формат → тираж → бумага → макет → срок.

**Стал:** **макет → что на макете → формат** → тираж → бумага → срок.

Второй вопрос новый — три кнопки с коэффициентом:

| Кнопка | К цене |
|---|---|
| Лёгкое: белый фон, текст, логотип | базовая |
| Стандарт: фото и цветные блоки | +30% |
| Плотное: фотоколлаж, тёмный фон | +70% |

Проверено расчётом на А4, 100 шт: 5 200 / 6 760 / **8 840 ₽**.
Раньше все три стоили бы одинаково.

Коэффициент идёт **отдельной строкой в счёте**, а не сноской — правило
от 17.08: наценка, которую не показали, не существует. И там же
оговорка: «категорию подтвердим, когда посмотрим файл».

## Поймана ошибка, которую сам и внёс

Сняв цены на 500 и 1000 штук, я оставил кнопки этих тиражей.
А `poTirazhu` при пустой ступени **молча берёт цену предыдущей** —
клиент выбрал бы 500 штук А4 и получил точную сумму 22 000 ₽
на работу, которую мы у себя не делаем.

Кнопки убраны. В приветствии сценария теперь сказано прямо: тиражи
от 500 штук печатаем через партнёрскую типографию, цена по запросу,
и объяснено почему — «её машина дешевле, и мы не перекладываем разницу
на вас».

**Урок:** снял цену из таблицы — проверь, что кнопка тоже исчезла.
Пустая ячейка не останавливает калькулятор, он берёт соседнюю.

**Сделано:**
- [x] `535551b` `_js-sekretar.html`: макет первым вопросом, шаг
      заполнения с коэффициентом, кнопки 500/1000 убраны
- [x] `535551b` `sekretar-ceny.mts` отдаёт категории заполнения в диалог
- [x] `535551b` синтаксис JS проверен, расчёт прогнан на живых данных

**Ждём от Гульнары:**
- [ ] **Загрузить папку `templates` целиком в Amvera** — иначе на сайте
      старый секретарь и старые цены. Папкой, не файлами по одному.
- [ ] Пройти заказ на живом сайте и проверить: первым вопросом макет,
      вторым — что на макете, кнопки 500 и 1000 пропали.

### 18 августа — секретарь сам считает краску по макету

Указание владелицы: «научи его определять количество краски, плотности
бумаги».

## Краска — считается автоматически, по самому файлу

Клиент прикладывает макет — **браузер рисует его на холсте 240 точек
и смотрит каждую точку: насколько она темнее белого.** Белый лист —
ноль краски, чёрная плашка — единица. Среднее и есть доля краски.

Всё считается **на компьютере клиента**, файл никуда не уходит.

**Проверено на живых картинках:**

| Что мерили | Результат |
|---|---|
| Белый лист со строкой текста | **1%** → лёгкое, коэффициент 1 |
| Логотип «Граната», бордо на весь кадр | **78%** → плотное, коэффициент 1,7 |

Результат сразу показывается человеку под полем файла: «краски
на макете 78% — плотное заполнение». И **уезжает в заявку строкой**,
чтобы владелица видела, на чём основана цена.

**Чего замер не умеет, и это написано в коде:**
- PDF, CDR, AI, EPS браузер не открывает — там честно говорим,
  что посмотрим глазами;
- жёлтая заливка тратит краску, но выглядит светлой, поэтому цифра —
  повод пересчитать, а не приговор;
- прозрачный PNG подкладывается на белое, иначе пустота считалась бы
  чёрной и белый макет показал бы 100%.

## Плотность — из файла не определить, но можно перестать спрашивать цифрами

Граммы человек не знает. Теперь рядом с каждой плотностью стоит
сравнение с предметом, который он держал в руках:

| Плотность | На ощупь |
|---|---|
| 115–130 г/м² | как страница глянцевого журнала |
| 150 г/м² | плотнее журнала, чуть держит форму |
| 160 г/м² | как хорошая рекламная листовка |
| 170 г/м² | как обложка тонкой тетради |
| 200 г/м² | как открытка из магазина |

**Сделано:**
- [x] `6dbbdbf` замер заливки в `_js-sekretar.html`, проверен headless-браузером
- [x] `6dbbdbf` результат замера уходит в заявку отдельной строкой
- [x] `6dbbdbf` подсказки «на ощупь» в `data/prices.ts` и на кнопках

**Ждём от Гульнары:**
- [ ] Загрузить `templates` целиком в Amvera и приложить на сайте
      любую картинку — под полем файла должно появиться «краски
      на макете N%».

### 18 августа — партнёрская печать ОТМЕНЕНА. Решение владелицы

Её слова: **«я не буду брать партнёрскую программу. Я лучше откажусь».**

Решение принято, обсуждение закрыто. Причина понятна и без объяснений:
посредничество — это чужое качество под нашим именем и зависимость
от типографии, с которой мы же и конкурируем.

## Что сделано вместо этого

**Большие тиражи вернулись в прайс — со своей честной ценой.**

| Формат | 50–100 | 200–300 | 500 | 1000 |
|---|---:|---:|---:|---:|
| А6 | 15 | 13 | 12 | 12 |
| Евро | 19 | 16 | 15 | 15 |
| А5 | 26 | 22 | 21 | 21 |
| А4 | 52 | 44 | **42** | **42** |

Убыточных позиций — 0.

**Отказывать молчанием мы не вправе.** Клиент видит цену и решает сам:
устроит — печатаем у себя, каждый лист под присмотром; не устроит —
уйдёт в офсет, и это нормально. Отказ за клиента, без названной цены,
это решение за него.

## Почему скидка за тираж такая маленькая — это надо уметь объяснять

У офсета дорогая приладка и дешёвый оттиск: тысяча выходит дешевле
сотни в разы. **У струйника наоборот — чернила и бумага тратятся
на каждый лист одинаково, экономится только приладка.**

Поэтому у нас 1000 штук почти по цене 500. Это не жадность, это другая
машина. Написано и в прайсе, и в приветствии секретаря — клиенту это
надо говорить прямо, иначе он сравнит с офсетной сеткой и решит,
что его обманывают.

## Заказ витражей по новому прайсу

500 листов А4, плотная заливка: 42 ₽ × коэффициент 1,7 = **71 ₽ за лист,
35 700 ₽ за тираж**. Материалы 22 698 ₽ — **прибыль 13 002 ₽**.

Против **минус 11 718 ₽** по старой цене 12 000 ₽.

**Но с этим клиентом цена уже согласована на 12 000 ₽**, и слово надо
держать. Новая цена — для следующего такого заказа, а этот остаётся
дорогим уроком. Решение по нему за владелицей.

**Сделано:**
- [x] `78d79c3` партнёрская печать убрана из прайса и из секретаря,
      кнопки 500 и 1000 вернулись с честной ценой
- [x] `78d79c3` в прайс и в приветствие добавлено объяснение, почему
      скидка за объём маленькая
- [x] `78d79c3` пересобраны секретарь и шпаргалка, синтаксис проверен

### 18 августа — «нет своих денег на перекуп». Настоящая причина, и она шире

Владелица объяснила отказ от партнёрской печати: **«у меня нет своих
денег пока перекупом заниматься»**.

Это не про качество подрядчика. Это про **оборотные средства**:
посреднику надо заплатить типографии вперёд и ждать расчёта клиента.

**И проблема шире партнёрства — она есть и на своих тиражах.**

## Считаем на заказе витражей по новой цене

| | Сумма |
|---|---:|
| Цена для клиента | 35 700 ₽ |
| Материалы, которые надо купить ДО печати | 22 698 ₽ — **64% от суммы** |
| Предоплата 30% | 10 710 ₽ |
| **Не хватает на материалы** | **11 988 ₽ из своего кармана** |

При выручке 1 362 ₽ за июль таких денег просто нет. **Заказ нельзя
даже начать** — не на что купить бумагу.

## ПРАВИЛО: предоплата обязана покрывать материалы

На малых заказах доля материалов 20–30%, и 30% предоплаты хватает
с запасом. **На тиражах от 200 штук доля доходит до двух третей.**

Заведено: `OPLATA.predoplataBolshojTirazh = 50`, порог 200 штук.
Секретарь считает сам и объясняет клиенту прямо: «тираж от 200 штук —
предоплата половина: бумагу и чернила на такой объём закупаем
до печати».

**Честно про остаток риска:** даже 50% на самом тяжёлом заказе
покрывают материалы на 79% — 17 850 ₽ против 22 698 ₽. Около 4 800 ₽
всё равно придётся вложить своих на несколько дней. Это уже подъёмно,
в отличие от двенадцати тысяч.

## ВЫВОД, который важнее правила

**Пока нет оборотных денег, большой тираж — самый рискованный заказ,
какой может прийти. Независимо от цены.**

Он требует вложить деньги вперёд и ждать. А заказ на грамоту,
сертификат или десять открыток не требует оборотных вовсе: материалов
там на сто рублей, они уже лежат на полке.

**Это ещё один довод за ту же витрину:** грамоты, сертификаты,
персональные открытки, тиснение фольгой. Высокая маржа, малый материал,
никакого кассового разрыва.

**Сделано:**
- [x] `691668c` `OPLATA.predoplataBolshojTirazh` — 50% на тиражи
      от 200 штук, с разбором в комментарии
- [x] `691668c` секретарь считает повышенную предоплату сам и объясняет
      её клиенту строкой в счёте

### 18 августа — бот ВКонтакте: цены теперь из одного источника

Указание владелицы: «надо ещё и бота в ВК научить, ему те же правила
передать».

## Сначала то, что нашлось в боте, и это плохо

`prices.py` бота писался 10.08.2026 и с тех пор отстал:

| Что у бота | Как есть на самом деле |
|---|---|
| множители `carton_300`, `carton_350` | **принтер не берёт плотнее 260 г/м²** — проверено живой печатью 17.08 |
| комментарий «картон 300 г/м² — базовая цена» у открыток | того же картона нет |
| листовки А4: 40 / 30 / 24 / 20 ₽ | цены сняты 18.08 как убыточные |
| нет наценки за 160 г/м² | заведена 17.08 |
| нет заполнения макета | главный множитель цены, ошибка до 70% |

**Бот прямо сейчас обещает клиентам картон, которого у студии нет.**

## Что сделано

Не стал править `prices.py` руками — иначе цифры разъедутся снова.
Сделан **генератор**: `npm run bot-ceny` собирает `bot/ceny_iz_prajsa.py`
из того же `data/prices.ts`, что и сайт.

Ровно то, чего требовала владелица 13.08: «мы можем подправить прайс
на сайте и забыть про бота. Это должно быть автоматическое обновление».

В файле: 22 строки цен (листовки, визитки), 8 множителей — плотности,
срочность и три категории заливки, новые допуслуги (ручная отделка,
жёсткая упаковка, доставка), правила предоплаты и список параметров,
которые бот обязан спросить ДО цены.

Файл проверен: синтаксис Python разбирается, значения читаются.

## ЧЕГО Я НЕ СДЕЛАЛ И ПОЧЕМУ — это важно

**Не тронул сам `prices.py`.** Причина в `CLAUDE.md`, раздел «чего
в репозитории пока нет»: файлы бота лежат в ветке
`claude/granat-kmv-multipage-site-6h04uz` и помечены как «11.08.2026,
может отставать» от того, что реально работает в Amvera.

**Править копию, которая старше рабочей, — значит откатить бота назад.**
Бот сейчас работает и принимает заказы; сломать его правкой вслепую
хуже, чем оставить со старыми ценами на день.

**Ждём от Гульнары, по порядку:**
- [ ] **Сначала выгрузить актуальные файлы бота из Amvera.** Приложение
      `jarvis-bot`, страница «Репозиторий», кнопка «Скачать данные».
      Прислать `prices.py`, `dialog_manager.py`, `knowledge_base.py` —
      тогда будет видно, что там на самом деле.
- [ ] После этого подключу `ceny_iz_prajsa.py` к настоящему боту
      и уберу картон 300 и 350.
- [ ] До тех пор **бот называет старые цены и обещает картон** —
      если придёт заказ на листовки через ВК, цену перепроверять руками.

**Сделано:**
- [x] `06b2653` `scripts/bot-ceny.mts` и команда `npm run bot-ceny`
- [x] `06b2653` `bot/ceny_iz_prajsa.py` — сгенерирован и проверен

### 18 августа — получен настоящий `knowledge_base.py` бота. Что он говорил клиентам

Владелица прислала файл из Amvera. **Копия в репозитории оказалась
актуальной** — дата в Amvera «7 days ago» совпала. Опасение снято.

## Что бот обещал клиентам, а студия дать не может

| Что говорил бот | Как на самом деле |
|---|---|
| Визитки: «база — мелованный **картон 300 г/м²**», «стандарт 300 или плотнее **350**» | принтер берёт до **260**, проверено живой печатью 17.08 |
| Открытки: «материал ТОЛЬКО плотный **картон 250–350**, базовый **300**» | того же картона нет |
| «Картон 250 → −10%, 300 → базовая, 350 → +10%» | множителей на несуществующий материал |
| Пример: «500 визиток на 300 г — **1750 ₽**» | такой цены в прайсе нет вовсе |
| Листовки: «от тиража цена **падает в несколько раз**» | правда для офсета, **ложь для струйника** |
| Предоплата **50%** везде | в прайсе 30%, и 50% только от 200 штук |
| Заполнение макета | **не спрашивалось вообще** — главный множитель цены |
| Макет | спрашивался **третьим** вопросом, после цены в голове клиента |
| Плотность 160 г/м², ручная отделка, упаковка, доставка | не было ни строки |

**Самое опасное — картон.** Бот звал клиентов ровно на ту бумагу,
на которой владелица намучилась в июле и испортила пачку.

## Что исправлено

- Картон 300 и 350 убран отовсюду, вместо него **запрет** с объяснением
  и с датой проверки — чтобы следующий чат не вернул обратно.
- В листовки добавлено **заполнение макета**: три категории
  с коэффициентами и объяснением, почему у струйника так.
- **Порядок вопросов у листовок изменён**: макет первым, заполнение
  вторым. По указанию владелицы: «макет надо просить раньше, чем
  обговаривать цену».
- Прямо запрещено обещать падение цены от тиража, с объяснением
  для клиента: «у струйника чернила тратятся на каждый лист одинаково».
- Предоплата: 30%, от 200 штук — 50%, с объяснением про закупку
  материалов.
- Добавлены 160 г/м², ручная отделка, жёсткая упаковка, доставка.

**Сделано:**
- [x] `ab2dada` `bot/knowledge_base.py` — исправлен, синтаксис Python
      проверен, 18 категорий на месте

**Ждём от Гульнары:**
- [ ] Прислать **`prices.py`** — там сами цифры, и они тоже старые.
- [ ] После этого загрузить оба файла в `jarvis-bot` и перезапустить.
- [ ] **До загрузки бот продолжает обещать картон 300 и 350.** Если через
      ВК спросят про визитки или открытки — поправлять руками.

### 18 августа — `dialog_manager.py`: поправлено три места

Владелица прислала второй файл бота. **Устроен он хорошо:** цены берёт
из `prices.py` через `prices.block(cat)`, а не выдумывает — расхождению
взяться неоткуда. Это правильная архитектура, её не трогал.

**Что исправлено:**

**1. Предоплата 50% была вшита прямо в шаблон ТЗ** (строка
«Оплата: предоплата 50%»). Прайс говорит 30%, а 50% только от 200 штук.
Бот писал в каждую заявку неверное условие. Заменено на правило.

**2. Добавлен блок «ЧЕГО СТУДИЯ НЕ МОЖЕТ ФИЗИЧЕСКИ»** — выше всех
прайсов, первым делом:
- бумага плотнее 260 г/м² не печатается, принтер не берёт;
- жёсткого мелованного картона нет вовсе;
- офсета нет, тиражи по своей цене, и она выше офсетной.

Раньше такого блока не было совсем: был только `config.NOT_OFFERED`
про услуги, но не про физический предел техники.

**3. Добавлено правило «макет спрашиваем раньше, чем называем цену»**
с объяснением про заливку и с ценой ошибки — 11 718 ₽ на живом заказе.
Оно общее для всех печатных категорий, а не только для листовок.

**Сделано:**
- [x] `0786d2c` `bot/dialog_manager.py` — три правки, синтаксис проверен

**Осталось получить от Гульнары:**
- [ ] **`prices.py`** — сами цифры. Без него бот считает по старым.
- [ ] **`config.py`** — там `TARIFFS`, `PAYMENT_TERMS`
      и `PAYMENT_TERMS_URGENT`. Предоплата 50% почти наверняка сидит
      и там тоже, и тогда бот будет спорить сам с собой.

### 18 августа — `prices.py`: бот считает по новому прайсу

Третий файл бота. Он подтвердил всё, что было видно по первым двум,
и добавил своё.

**Что бот считал неверно до этой правки:**

| Что | Было у бота | Стало, как на сайте |
|---|---|---|
| Листовки А4 | 40 / 30 / 24 / 20 ₽ | 52 / 44 / 42 / 42 ₽ |
| Листовки А6 | 15 / 12 / 10 / 8 ₽ | 15 / 13 / 12 / 12 ₽ |
| Заливка макета | не спрашивал вовсе | лёгкая / +30% / +70% |
| Картон 300 и 350 г/м² | предлагал как материал | убраны, предел 260 |
| Глиттер | 50 ₽ и 100 ₽ за штуку | ручная отделка 150 ₽ |
| Жёсткая упаковка | нет строки | 50 ₽ за штуку |
| Доставка | нет строки | 200 ₽, от 5 000 ₽ бесплатно |
| Тираж 101–199 штук | строки нет, «зови менеджера» | считается |
| Сертификат, 1 штука | минимум 700 ₽ | 200 ₽, минимум 150 ₽ |

Последняя строка — тот самый заказ, что ушёл 16 августа. Бот отвечал
человеку с одной грамотой «минимум по разделу 700 ₽», потому что
сертификаты лежали в открытках и попадали под их минимум. Теперь это
отдельный вид `certificate`: печатаем от одной штуки, розничный
минимум 150 ₽.

**Заливка применяется сама.** Раньше модель должна была и спросить про
заливку, и не забыть передать коэффициент в расчёт. Забыла бы — заказ
посчитался по лёгкой цене, и это ровно тот убыток, ради которого
коэффициент заведён. Теперь достаточно параметра `fill`, множитель
подставляет код.

**В самопроверку добавлен счёт заказа.** До сих пор `python prices.py`
проверял только, УЗНАЛ ли бот услугу по словам клиента. Сколько он за
неё возьмёт, не проверял никто — прайс расходился с сайтом молча, и
увидеть это можно было только живым разговором, то есть деньгами.
Теперь десять заказов считаются числами, включая витражи: 500 листов
А4 с плотной заливкой — 35 700 ₽ (по старой цене было 12 000 ₽ при
материалах на 22 698 ₽).

**Сделано:**
- [x] `c4e22c2` `bot/prices.py` — цены, заливка, сертификаты, доставка
- [x] `c4e22c2` `bot/knowledge_base.py` — сертификаты и заливка в бриф
      открыток

**Ждём от Гульнары:**
- [ ] Загрузить в `jarvis-bot` **четыре файла целиком**: `prices.py`,
      `knowledge_base.py`, `dialog_manager.py`, `config.py` —
      и перезапустить.
- [ ] **До загрузки бот в ВК и Telegram считает по старым ценам** —
      листовки А4 по 40 ₽ при себестоимости выше. Если через ВК придёт
      заказ на листовки, считать руками.

**Не доделано в коде:**
- [ ] Расход чернил измерить по факту (10 листов, уровни до и после),
      когда придут чернила 22 августа. Вся себестоимость пока на оценке.
- [ ] `config.TARIFFS` не сверен с прайсом — файла ещё нет.

### 18 августа — `config.py`: предоплата 50% нашлась и здесь

Четвёртый файл бота, последний. Предположение подтвердилось: **50%
были вшиты и сюда**, третьим местом подряд. Шаблон ТЗ писал «предоплата
50%», прайс на сайте — 30%, а бот в разговоре — снова 50%. Клиент мог
услышать три разных условия в одном заказе.

**Чем этот файл опасен.** `TARIFFS` в нём — запасная таблица: заказ
считает `prices.py`, а сюда бот заглядывает только в двух случаях —
категории нет в прайсе (визитки, меню, широкоформат, весь веб) либо
`prices.py` не загрузился, импорт там под `try/except`, чтобы бот
не падал. То есть **расхождение заметить нельзя**: пока файл на месте,
устаревшие цифры молчат. В тот день, когда `prices.py` не доедет
в Amvera, бот начнёт считать по ним — и никто не поймёт, почему цены
вдруг стали убыточными.

Именно поэтому таблицы сведены целиком, а не только те, что «работают».

**Что исправлено:**

- **Предоплата** — 30%, а от 200 штук 50%.
- **Визитки** — таблица была на картоне 300 и 350 г/м², которого
  у студии нет. Заменена на бумагу 250–260 и новые цены: 4+0 —
  500 / 750 / 1400 / 3250 ₽, 4+4 — 900 / 1650 ₽ и «по запросу».
  Старая говорила 450 ₽ за сотню там, где теперь 750 ₽.
- **Листовки** — новые цены плюс правило про заполнение макета
  и бумага 160 г/м², которая лежит на складе.
- **Открытки** — картон 300 и 350 убраны, добавлены А4, сертификаты
  от одной штуки, ручная отделка, жёсткая упаковка, фольга, доставка.

**Одно место оставлено как есть и помечено.** `PAYMENT_TERMS_URGENT`
требует 100% предоплаты на срочном заказе — решение студии от
10.08.2026 с разумным объяснением: срочность двигает очередь, бумага
режется сразу, а перепродать нарезанный тираж с чужим именем некому.
Но **на сайте и в печатном прайсе этого условия нет**: там про
срочность сказано только «плюс 30%». Не отменяю без слова владелицы,
однако пока условие живёт в одном канале из двух, клиент услышит
от бота то, чего не нашёл в прайсе.

**Сделано:**
- [x] `6fcf2e6` `bot/config.py` — предоплата, визитки, листовки, открытки

**Ждём от Гульнары:**
- [ ] Решить про **100% предоплаты на срочном заказе**: оставляем
      и дописываем в прайс на сайте — или снижаем до 50%.

### 18 августа, вечер — заказ на 12 000 ₽ и три ошибки, которые он показал

Днём пришла живая заявка: 500 листовок А4, двусторонние, **12 000 ₽**.
Заказ от «Студии Художественных Витражей», Пятигорск. Клиентка приезжала,
смотрела макет, заплатила 300 ₽ за макет и написала, что предоплату
привезёт наличкой.

**Цена убыточна, и это посчитано, а не на глаз.** Макет плотный с обеих
сторон — коллаж из фотографий на лице и **сплошной чёрный фон** на
обороте. По новому прайсу это 71 ₽ за лист, то есть 35 700 ₽.

| Что | Сумма |
|---|---|
| Чернила, 1 100 мл по оценке | 17 466 ₽ |
| Бумага 130 г/м², с запасом | 5 232 ₽ |
| Налог 6% и эквайринг 2,5% | 1 020 ₽ |
| **Расходы** | **23 718 ₽** |
| Цена клиенту | 12 000 ₽ |
| **Убыток** | **−11 718 ₽** |

**Решение владелицы: печатать по 12 000 ₽.** Её слова: «цену обговорили,
это наша ошибка, это работа, мы не предусмотрели эти ошибки». Цену
называла студия, а не сайт, — валить на старый прайс нечестно.
Решение записано как есть, спорить не с чем.

**Что сказано ей в ответ, чтобы не встать посреди тиража:**

1. **Чернил физически не хватит.** На тираж нужно около 1 100 мл,
   в комплекте за 6 669 ₽ — 420 мл. Это два с половиной комплекта,
   а оплачен один и приедет 22 августа. Раньше 23–24 августа тираж
   не выйдет.
2. **Светлый оборот экономит около 4 700 ₽ и цену клиентки не трогает.**
   Довод для неё технический, а не денежный: сплошной чёрный на 130 г/м²
   долго сохнет, лист ведёт, идут полосы.

**Три ошибки, которые вскрыл этот заказ.** Все три починены сегодня.

**Первая — форма принимала ОДИН файл.** Клиентка написала прямо: «там
загружается только одна картинка, а мне нужно отправить две стороны».
Она приложила лицо, увидела, что файл прикрепился, и была уверена, что
отправила обе. Печатать было нечего, и выяснилось это через сутки —
уже после того, как цену обсудили. **Это не её ошибка, а наша форма.**

**Вторая — секретарь про две стороны не говорил вообще.** Ни в диалоге,
ни подписью под полем. Теперь говорит первым делом, как только клиент
отвечает «макет готов».

**Третья — расширения в форме и на сервере разошлись.** Сервер принимал
`.zip` и `.rar`, а окно выбора файлов их не показывало: человек видел
свой архив серым и решал, что архивы мы не берём.

**Плюс четвёртая, найденная на живом расчёте:** секретарь говорил
клиенту «предоплата 30%» на тираже 500, а в заявке владелице уходило
50%. Два разных условия в одном заказе.

**Сделано:**
- [x] `4b0f30a` предоплата 50% называется прямо в диалоге, на тираже от 200
- [x] `17d0898` макет приложить можно четырьмя файлами; заливка считается
      СРЕДНИМ по сторонам, а не по худшей — белый оборот при плотном лице
      действительно дешевле
- [x] `21ab555` в окне выбора файлов видно архивы
- [x] `eb88128` секретарь сам просит обе стороны макета

**Ждём от Гульнары:**
- [ ] Загрузить в `granat-site` папку `templates` целиком, `app.py`
      и `pricegranat.html`, включить пересборку. **`app.py` в этот раз
      менялся** — без него сервер возьмёт один файл из двух.
- [ ] Проверить на живом сайте: приложить два файла и посмотреть, что
      в письме пришли оба вложения.
- [ ] Решить с клиенткой срок: раньше 23–24 августа тираж не выйдет.
- [ ] Предложить ей светлый оборот — экономия 4 700 ₽ при той же цене.

**Правило на будущее, самое дорогое за день.** Клиент не виноват, когда
форма молчит. Он видит, что файл прикрепился, и считает, что сделал всё.
Каждый раз, заводя поле, спрашивать себя: **сколько всего человек может
захотеть сюда положить и узнает ли он, что положил не всё.**

### 18 августа, ночь — предоплата считалась в трёх местах

Владелица прошла секретаря на живом сайте и увидела: «в переписке
секретарь говорит 50%, а на выходе 30%». На тираже 500 экран оплаты
показывал 10 710 ₽ вместо 17 850 ₽.

Причина не в опечатке. **Процент предоплаты вычислялся в трёх местах** —
в вопросе «оформляем по этой цене?», в заявке владелице на почту и на
экране оплаты. Правило «от 200 штук половина вперёд» завели утром
в первых двух, а третье осталось на базовых 30%.

**Правило: одна цифра, названная в трёх местах, рано или поздно
разъедется в двух.** Теперь считает одна функция `procentPredoplaty()`,
и на экране оплаты сказано, ПОЧЕМУ половина.

**Сделано:**
- [x] `b09e255` процент предоплаты считается в одном месте

**Не выкачено на сайт.** Владелица закончила проверки на сегодня.
В `granat-site` эта правка ещё не загружена: живой сайт показывает
на экране оплаты 30% и на большом тираже. Загрузить `templates`
целиком следующей выкаткой.

### Незакрытый вопрос: на сайте ТРИ места для заявки

Владелица: «на Амвере надо поправить, оставить заявку эту и всё
остальное удалить». Что именно оставить — не уточнено, спросил и ответа
пока нет. Мест сейчас три:

1. **Секретарь Джарвис** — собирает параметры, считает цену, принимает макет.
2. **Форма «Оставить заявку»** — блок внизу, куда ведёт золотая кнопка
   в шапке. Только имя и телефон.
3. **Симулятор** на странице «Сайты и боты» — из-за него письма приходят
   с заголовком «ЗАЯВКА ИЗ СИМУЛЯТОРА», хотя заявка пришла от секретаря.

**Ждём от Гульнары:**
- [ ] Решить, какое место оставить. Если секретаря — попутно перенаправить
      золотую кнопку в шапке на него и переименовать заголовок письма
      на «ЗАЯВКА С САЙТА».

### 18 августа, ночь — счётчик заявок крутили проверки

Владелица: «у нас уже 27-я заявка, а по сути только одна». Каждая
проверка секретаря забирала номер.

**Обнулять счётчик нельзя, и это важно.** Номера уже стоят в журнале
`/data/leads.jsonl` и в именах файлов с макетами на диске. Начни счёт
заново — новая заявка №5 склеится со старой №5, и макет одного клиента
ляжет к заказу другого.

Поэтому не обнуляем, а перестаём тратить: заявка с телефона студии
номера не получает, в журнал заявок не ложится, а письмо приходит
с пометкой «🧪 ПРОВЕРКА, НЕ КЛИЕНТ». Другие номера для проверок можно
добавить переменной `TEST_PHONES` в Amvera, через запятую.

Заодно заголовок письма от секретаря стал «ЗАЯВКА С САЙТА»:
«ИЗ СИМУЛЯТОРА» осталось от старого блока и путало — присылал-то
секретарь.

**Сделано:**
- [x] `856a92e` `zhivoj-sajt/app.py` — проверки не тратят номера

**Ждём от Гульнары:**
- [ ] Загрузить в `granat-site` свежий `app.py` и папку `templates`.
      До загрузки счётчик крутится по-прежнему.

### 18 августа, ночь — техзадание, сброс счётчика, последняя выкатка

Три правки после разбора заказа на 12 000 ₽.

**1. Техзадание с подписью «Согласовано».** Требование владелицы: «секретарь
после всех вопросов предлагает: прочитайте внимательно техническое задание
и поставьте подпись согласовано». Теперь перед телефоном секретарь
выкладывает весь заказ одним списком и ждёт нажатия. Дата и время
согласования уезжают в заявку. «Нужно поправить» — считаем заново,
человека не зовём.

Честная оговорка, сказана и владелице: **нажатие кнопки не подпись
в юридическом смысле.** Это след в заявке, которого хватает для разговора
«я не то заказывала», но не для суда. Настоящую подпись даёт счёт.

**2. Двойной вопрос про макет.** Утром вопрос перенесли в начало сценария,
а со старого места не убрали — клиента спрашивали дважды, и цену считал
ВТОРОЙ ответ, затирая первый. Владелица увидела это в сводке заказа:
две строки «Макет» подряд, причём с разными ответами. Правило: перенёс
шаг — проверь, что на старом месте его нет.

**3. Обнуление счётчика заявок — через архив.** Владелица попросила
обнулить; объяснил, почему нельзя писать ноль поверх, и сделал безопасно:
`/api/sbros-schetchika` сначала уносит журнал и макеты в архив с датой
в имени и только потом обнуляет счётчик. Не удаляется ничего. Архивация
не удалась — счётчик не трогается. Доступ по `WATCH_TOKEN`.

**Сделано:**
- [x] `14d8389` техзадание и подтверждение «Согласовано»
- [x] `1746a27` про макет спрашиваем один раз
- [x] `556432f` сброс счётчика с архивом
- [x] Всё выкачено в `granat-site` вечером 18.08, пересборка сделана

**Ждём от Гульнары:**
- [ ] **Реквизиты для счёта:** полное имя с отчеством, ОГРНИП, расчётный
      счёт, банк, БИК, корсчёт, и какой налог — НПД или УСН 6%. Положить
      их в переменные Amvera, а не в открытый репозиторий.
- [ ] Второй телефон `+7 928 352-89-99` добавить в переменную `TEST_PHONES`
      приложения granat-site и перезапустить — иначе проверки с него
      тратят номера заявок. Заявка №28 ушла именно так.
- [ ] Запустить сброс счётчика ссылкой, когда решит.
- [ ] Проверить приложение ДВУХ файлов макета: под полем должно
      появиться «Приложено 2» и среднее по двум сторонам.

**Не доделано в коде:**
- [ ] Счёт на оплату с реквизитами — ждёт данных владелицы.
- [ ] Три места для заявки на сайте (секретарь, форма, симулятор) —
      владелица просила оставить одно, какое именно, не решено.

### 18 августа, ночь — потеряли клиента на распечатке документов

Позвонила девушка из Краснодара: распечатать готовые документы, водитель
уже ехал забирать. Заполнила заявку на сайте — **приложить файл секретарь
не предложил, а в конце пообещал связаться позже.** Ответ: «спасибо,
не надо, я в другом месте».

Слова владелицы: «какие ещё минус-клиенты у меня будут с таким умным
секретарём, каждый день убытки». Справедливо: самый простой заказ
на свете, и он не прошёл.

**Три причины, все в коде:**

1. **Поле называлось «Макет».** Человек с документами на копию слово
   «макет» глазами не ищет. Поле было на экране — и его не увидели.
2. **Секретарь про файл молчал** и надеялся, что поле заметят внизу.
   Про обе стороны листовки его научили утром, про документ — нет.
3. **Конец разговора был один на все услуги** — «перезвоним в рабочие
   часы». Для тиража верно, для распечатки это приговор: человек готов
   приехать через час.

**Проверил остальные сценарии, не дожидаясь следующего ушедшего.
Нашлась ещё одна дыра, хуже первой: «Другая задача» упиралась в тупик.**
Секретарь говорил «расскажите задачу», а рассказать было НЕКУДА —
в форме только телефон, согласие и файл. Человек уходил молча, и студия
даже не узнавала, чего он хотел.

**Сделано:**
- [x] `13a744a` поле называется «Файл — приложите прямо здесь: документ,
      макет, фото»; секретарь просит файл словами; на копиях, фото
      и ламинации конец разговора — «печатаем сегодня, приезжайте»
      с адресом и телефоном, а в нерабочее время «напечатаем к открытию»
- [x] `429126e` поле «своими словами» в форме, обязательное для «Другой
      задачи»; текст уезжает в заявку строкой «Клиент написал: …»;
      фото на документы тоже зовёт прийти, а не обещает перезвонить

**Не доделано в коде:**
- [ ] **Оплата онлайн до звонка.** Пока путь такой: цена → файл →
      заявка → человек. Нужно: цена → файл → оплата по QR → приехать
      забрать. QR уже есть, показывается в конце; для мелких заказов
      он должен появляться сразу.

**Правило дня, самое дорогое:** называть поля словами КЛИЕНТА, а не
своими. «Макет» — слово типографии. Человек несёт документ, фотографию,
файл. И проверять каждый сценарий целиком, а не только тот, который
сегодня правили.

## 19 августа

**Цель дня:** закрыть дыры, из-за которых секретарь терял живых клиентов,
и завести проверки, чтобы следующие такие же находились до клиента.

### Потерянный клиент и разбор всей дороги

Позвонила девушка из Краснодара: распечатать готовые документы, водитель
уже ехал забирать. Заполнила заявку на сайте — приложить файл не смогла,
а в конце получила обещание связаться позже. Ушла со словами «спасибо,
не надо, я в другом месте».

**Нашлось, каким путём она шла, и это оказалось не догадкой.** Золотая
кнопка «Оставить заявку» в шапке вела МИМО секретаря — в старую форму
внизу страницы: ни расчёта, ни поля для файла, а под кнопкой подпись
«Отвечаем в течение дня». Секретарь, который спрашивает и считает, стоял
выше на той же странице; до него просто не доводили.

Форма стояла не на двух страницах, а на пяти: главная, печать, контакты,
работы, «Как работаем».

**Сделано:**
- [x] `13a744a` поле «Файл — документ, макет, фото»; секретарь просит
      файл словами; на копиях, фото и ламинации конец разговора —
      «печатаем сегодня, приезжайте» с адресом, а в нерабочее время
      «напечатаем к открытию»
- [x] `429126e` поле «своими словами», обязательное в «Другой задаче»:
      секретарь просил рассказать задачу, а рассказать было НЕКУДА
- [x] `76832aa` кнопка ведёт к секретарю, форма перестала обещать
      «в течение дня»
- [x] `3f2be07` форма убрана со страниц печати; заодно найдено, что сайт
      обещал «предоплату 50%» при прайсовых 30% и «печать от 1–2 дней»
      при сроке 3–5 рабочих
- [x] `f908d7c` секретарь вместо формы на контактах, работах и «Как работаем»
- [x] `e33ee7e` `ef3dd8a` кнопка «Узнать цену и заказать» с целью Метрики

### Распечатка документов доведена до конца

Владелица: «надо было спросить сколько страниц, с двух сторон печать или
с одной, приложите документы, печать с двух сторон стоит сколько».

- [x] `1794bdf` число страниц вводится ЧИСЛОМ, а не кнопками «1–5 / 10 /
      50»; отдельный вопрос про стороны; секретарь объясняет счёт словами
- [x] `7621bb0` про заливку молчим там, где она на цену не влияет:
      владелица приложила два PDF к распечатке и увидела «заливку
      посмотрим глазами»
- [x] `76f35ce` формулировка исправлена: не «браузер не читает» — PDF
      браузер открывает прекрасно, это НАШ замер не умеет

### Кто забирает, к какому часу, и что остаётся клиенту

- [x] `ffcc7d2` секретарь спрашивает, кто заберёт заказ; выбрали курьера —
      появляется поле для его телефона. Час готовности спрашивается всегда
- [x] `4b1a2b4` «срочно сегодня» стало «сегодня, к определённому часу»:
      час обязателен, и секретарь честно считает, сколько до закрытия
- [x] `0831703` **клиент сохраняет свою заявку** — ссылка на памятку
      с номером, составом, суммой и адресом. До этого он не получал
      ничего: закрыл вкладку и остался ни с чем
- [x] `0831703` мелкий заказ до 1000 ₽ оплачивается ЦЕЛИКОМ И СРАЗУ.
      Решение владелицы: «такие заказы надо сразу оплатить». Просить
      20 ₽ предоплаты с заказа в 65 ₽ бессмысленно, а «потом на месте»
      не годится — распечатанные страницы с чужой фамилией не продать

### Счёт, реквизиты и налоговый режим

- [x] `56f44a5` счёт на оплату по подписанной ссылке: собирается
      из заявки, сумма прописью, телефон клиента в документ не попадает
- [x] `58fe731` назначение платежа, срок оплаты, место под ОГРНИП
- [x] `f7bdd70` **режим НПД, а не УСН.** Касса не нужна, чек из «Мой
      налог» обязателен. Налог 4% с физлиц вместо 6% — все прошлые
      расчёты себестоимости были не в пользу студии
- [x] `963d7c3` проверка себестоимости берёт ставку из прайса

### Оплата и реклама

- [x] `e2adb8e` под QR написано, что кода из СМС не будет: 18.08 клиентка
      отказалась платить, потому что ждала код из банка. По СБП его нет
- [x] `f20205a` короткие адреса `granat-kmv.ru/2gis`, `/ya`, `/karty`,
      `/vizitka` — ведут сразу к секретарю и сами ставят метку

**Владелица: «с рекламы звонят и говорят — я не знаю о вашем сайте
ничего».** В карточках 2ГИС и Яндекс Бизнеса стоит телефон, ссылки
на сайт нет.

### Проверки вместо надежды

Владелица: «я откуда знаю, что ещё может вылезти». Ниоткуда — и знать
не должна.

- [x] `e2adb8e` `npm run sekretar-proverka` проходит все восемь сценариев
      и ищет: вопрос, заданный дважды; сценарий без срока; ответ, который
      уедет в заявку без названия; запрещённые обещания.
      **На первом запуске нашла две штуки, которые ассистент пропустил:**
      «Бумага» уходила в заявку голым ответом, а подписи `kolvo`
      и `storony` были заданы дважды — «Тираж» молча стал «Количеством»
      во всех сценариях сразу
- [x] `c69a726` в письме «Файлов приложено 2», а не «Макетов»: человек
      нёс документы. Убрано «Ждём клиента в боте для сбора ТЗ»

**Ждём от Гульнары:**
- [ ] Вставить ссылку на сайт в карточки 2ГИС и Яндекс Бизнеса.
      2ГИС: филиал → «Контакты» → тип «Сайт». Яндекс Бизнес: «Профиль
      организации» → «Контакты» → «Сайт». Короткие адреса заработают
      после выкатки `app.py`
- [ ] Второй телефон `+7 928 352-89-99` в переменную `TEST_PHONES`
- [ ] ОГРНИП для счёта — строка есть, значение пустое
- [ ] 22 августа замерить реальный расход чернил

**Не доделано в коде:**
- [ ] Оплата онлайн ДО звонка для мелких заказов: QR показывается
      в самом конце, а мелкий заказ должен оплачиваться сразу
- [ ] Плательщик-организация в счёте: сейчас «Физическое лицо». Для
      юрлица нужны его название и ИНН — секретарь их не спрашивает
- [ ] Симулятор на «Сайтах и ботах» — третья точка входа, тоже без файла

**Правило дня.** Называть поля словами КЛИЕНТА, а не своими. «Макет» —
слово типографии; человек несёт документ, фотографию, файл. И проверять
весь сценарий целиком, а не только тот кусок, который сегодня правили.

### 19 августа, день — скидка пенсионерам, страница документов, счёт клиенту

**Скидка пенсионерам 10%** — на открытки, сертификаты и грамоты. Повод
живой: «мать хочет дочери открытку написать». Владелица дважды уточнила
границы: сначала «для распечатки и копии нет, а для открыток
поздравительных да», потом «открытки, сертификаты, грамоты».

Копии и распечатка сюда НЕ входят намеренно: копия паспорта стоит 10 ₽,
и скидка в рубль ничего не значит для человека, а заработок съедает.

**Скидка не вычитается калькулятором.** Подтверждает её удостоверение
на месте, а автоматическую поставит себе каждый, кто дошёл до цены.
Секретарь про неё ГОВОРИТ строкой, но в счёт не вносит.

**Сделано:**
- [x] `7e4f06a` `71edfb3` скидка в прайсе, секретарь пересобран
- [x] `d6289ca` страница `/dokumenty` — копии документов для иностранных
      граждан. Владелица: «надо, чтобы в поиске выскакивала наша фирма».
      Цена та же, что у копирования: страница нужна не ради другой цены,
      а ради других слов — паспорт, миграционная карта, уведомление
      о прибытии, патент, РВП, вид на жительство
- [x] `d6a810b` заголовок и описание сайта говорят про действие:
      «рассчитать и заказать печать онлайн», а не только про товар
- [x] `815b488` **заявки не считались в Метрике ни разу**: код слал
      `lead_form`, а цель заведена как `zayavka`. Отсюда нули
      в конверсиях, на которые владелица смотрела каждый день
- [x] `745d920` срочный заказ спрашивает час кнопками в разговоре
- [x] `0e6d31f` в репозиторий положены `bots.py`, `contact.py`,
      `leads.py`, `fraud_check.py` — сайт наконец лежит целиком

**Про панель Amvera.** Днём «Репозиторий» и «Скачать данные» висели
по десять минут. Сайт при этом работал: `/api/health` отвечал, главная
открывалась. **Важно помнить: `/api/health` НЕ проверяет страницы** —
15.08 он отвечал `ok`, когда сайт не показывал ничего. Проверка сайта —
это адрес без хвоста.

**Выкатку пушем в `amvera` делать НЕЛЬЗЯ**, хотя подключение есть.
В репозитории нет картинок портфолио, логотипа, `privacy.html`
и `consent.html` — они лежат в корне приложения и раздаются оттуда.
Пуш их сотрёт: сайт откроется без фотографий и без страницы политики,
а она обязана быть по 152-ФЗ. Сначала положить их в репозиторий.

**Ждём от Гульнары:**
- [ ] Цена за «вписать текст в открытку». Ближайшее в прайсе —
      адаптация макета 300–500 ₽, отдельной строки нет
- [ ] Цена за помощь с подачей заявления через Госуслуги, если решит
      её оказывать. Название только «помощь в заполнении и подаче»,
      не «оформление прописки»: результат обещать нельзя, а фиктивная
      регистрация иностранцев — статья 322.3 УК
- [ ] Ссылка на сайт в карточки 2ГИС и Яндекс Бизнеса

### 19 августа, вечер — текст на изделии и подарок к нему, потом стоп

**Текст в открытку.** Живой вопрос от клиентки: «женщина для открытки
хочет вставить текст. есть возможность?» — а секретарь такого шага
не имел вовсе. Теперь спрашивает: печатать текст на изделии или нет,
и если да — принимает его прямо в разговоре.

**Подарок к тексту.** Владелица вспомнила, как это делал бот
в Telegram: «спрашивал имя, возраст, характер, чего добилась, каких
успехов, особенности» и предлагал сгенерировать текст за 40 ₽. Тот же
приём вернули в секретаря: после согласия печатать текст он предлагает
помощь с самим поздравлением.

**Сделано:**
- [x] `74d4851` из подарков оставлены две позиции — короткий текст
      поздравления 40 ₽ и стих или поздравление 190 ₽. Песня
      и сказка-раскраска не удалены, а выключены `aktivna: false`

**Почему выключены.** Слова владелицы: «не надо пока сказки раскраски
и песни. пока повременим. надо чтобы это всё уже встало ровно и без
проблем». Это верно и по делу: за день секретарь получил вложения
документов, шаг про страницы, счёт, памятку клиенту, скидку
пенсионерам и текст на изделии — всё это ещё не проверено живыми
заказами. Новое сюда добавлять нельзя, пока старое не отработало.

Записи оставлены в прайсе намеренно: включаются обратно одной
строкой, когда владелица скажет.

**Остановились на:** выкатили `74d4851` в `granat-site`, проверяем
живым сайтом по адресу `/dokumenty` — этой страницы раньше не было,
поэтому она и есть признак новой версии.

**Ждём от Гульнары:** то же, что днём, плюс
- [ ] Добавить `+7 928 352-89-99` в `TEST_PHONES`, чтобы её
      собственные проверки не съедали номера заявок

**Не доделано в коде:**
- [ ] Картинки, `privacy.html` и `consent.html` в репозиторий — без
      них выкатка пушем невозможна
- [ ] Замер реального расхода чернил 22.08: все расчёты
      себестоимости пока стоят на оценках

### 19 августа, вечер — страницы под сертификаты и открытки

Владелица посмотрела на `/dokumenty` и сказала: «про сертификаты
и открытки тоже есть». Верно: за грамотой и сертификатом к нам идут
чаще всего — 16.08 первый живой клиент с Карт просил ровно ОДИН
сертификат. А слов «напечатать грамоту» и «приглашения на свадьбу»
на общей странице печати нет вовсе, они спрятаны в примечании
к таблице открыток. По таким запросам страница не показывается.

**Сделано:**
- [ ] `/sertifikaty` — сертификаты, дипломы, грамоты,
      благодарственные письма. От одной штуки, розничный минимум 150 ₽
- [ ] `/otkrytki` — открытки, приглашения, поздравления. Таблица
      по форматам, свой текст на изделии, помощь с поздравлением
- [ ] на общей странице печати обе карточки получили ссылки

**Заодно нашлась разошедшаяся цена.** В карточке «Печать сертификатов»
стояло «От 300 ₽», при том что розничный минимум на сертификат
и грамоту — 150 ₽ от одной штуки, решение владелицы от 17.08. Вдвое.
Сайт отпугивал ценой, которой в прайсе нет. Исправлено там же.

**И лишнее обещание.** В карточке приглашений было «конгревное
тиснение золотой фольгой» и «сургучная печать». Конгрев — это рельеф
прессом и стальным клише, его у нас нет; фольгирование есть и стоит
300 ₽ за лист. Оставлено то, что делаем.

**Проверка текстов поумнела.** `npm run proverka` кричал «СТОП»
на скидку пенсионерам — он знал только про массив `AKCII`, а скидка
живёт отдельной постоянной записью. И не видел призыва к действию
на страницах сайта: там CTA — сам секретарь, он подключается строкой
`include`. Оба случая научены. Проверка, которая кричит зря,
перестаёт читаться — это дороже пропущенной ошибки.

**Остановились на:** страницы написаны и проверены скриптом, шаблоны
разбираются, `app.py` компилируется. Ждём выкатки — файлы те же:
папка `zhivoj-sajt` целиком.

**Хвост:** `sitemap.xml` лежит в корне приложения и в репозитории
его нет, поэтому три новые страницы туда не попали. Пока их не будет
в карте сайта, поисковик найдёт их только по ссылкам с других страниц —
медленнее. Быстрый обход: Яндекс Вебмастер → «Переобход страниц».

**Цена сертификата: владелица остановила выкатку и была права.** На новой
странице стояло «от 150 ₽», и она спросила, когда эта правка делалась,
добавив: «фотобумага, чернила, у меня не цифровая печать, всё посчитано».

Разбор по истории: 150 ₽ появились коммитом `a118cfb` 17.08 в 01:06 —
это **розничный ПОРОГ**, а не цена А4. Он снимал минимум раздела
открыток, из-за которого один сертификат стоил бы 1 200 ₽. Цена А4 —
200 ₽ штучно и 180 ₽ от десяти, из `prices.py` бота с 11.08.

Решение владелицы 19.08.2026, глядя на блок цен живого сайта: «вот
смотри от 180 рублей. я думаю что это правильно». Витрина теперь
говорит «от 180 ₽», порог 150 остаётся в расчёте и на витрину
не выносится — он читается как цена.

**Найдено попутно:** в блоке цен на сайте у сертификатов до сих пор
стояло «штучный заказ — от 1200 ₽», отменённое 17.08. То есть страница
цен противоречила и прайсу, и секретарю. Исправлено.

**Открытый вопрос — заполнение макета.** Коэффициент заливки
(до 20% базовая, 20–60% плюс 30%, больше 60% плюс 70%) применяется
ТОЛЬКО к листовкам. Сертификаты и открытки считаются одной ценой при
любой заливке, и секретарь про макет там не спрашивает — хотя сертификат
почти всегда плотный: рамка, фон, вензель. Чернила по её закупке 18.08
стоят 15,88 ₽ за мл, при плотной заливке это около 17,5 ₽ краски
на лист А4 (оценка, не замер).

**Не доделано в коде:**
- [ ] Плотной фотобумаги 200–249 г/м² нет в себестоимости вовсе — только
      мелованная 130 г/м² по 8,72 ₽ за лист. Пока её нет, посчитать
      сертификат нечем. Нужна цена пачки и число листов
- [ ] Решить, считать ли сертификаты и открытки по шкале заполнения

**Копия и распечатка — одна цена 12 ₽, минимума больше нет.** Решение
владелицы 19.08.2026 в два хода: сначала «я один лист распечатывала
за 12 рублей, и мне это оплачивали», потом на вопрос «сводим?» —
«12 рублей», и следом «а если там много… от 50 листов тогда уже пиши,
как ты писал».

Было две строки — копия с бумаги 10 ₽ и печать с файла 12 ₽ — плюс
минимальный заказ 50 ₽. Две беды сразу. Человеку у стойки приходилось
решать, чем он к нам пришёл, ради двух рублей разницы. А минимум
требовал с него 50 ₽ за один лист при том, что студия берёт 12 —
вчетверо, и первым это услышал бы тот, кто пришёл распечатать
одну страницу.

Стало: А4 ч/б — 12 ₽ до 49 листов, 10 ₽ от пятидесяти, 9 ₽ от сотни.
Ступени по количеству оставлены, их владелица подтвердила отдельно.
То же по А3. Минимума нет.

**Сделано:**
- [ ] `data/prices.ts` — строки сведены, минимум снят, витрина
      пересчитана на «от 9 ₽»
- [ ] секретарь: две кнопки вместо трёх, названия совпадают
      с прайсом. Проверка сценариев проходит
- [ ] `/dokumenty`, блок цен и описание страницы печати — везде новые
      ступени
- [ ] шпаргалка и цены бота пересобраны

**Почему это всплыло только сейчас.** Владелица открыла живую страницу
и спросила «10 или 8 или 12? цена». Три цифры на одном сайте про одну
услугу — это не расхождение в коде, а расхождение в голове клиента.
Цена, которую надо объяснять, работает против нас.

**Страница, а не лист.** Владелица спросила в одно слово: «лист или одна
страница?» — и оказалась права, слово в прайсе было неверное. Секретарь
всегда считал по страницам, то есть по сторонам: краска и прогон уходят
на каждую сторону. А таблица называлась «цена за 1 лист», и человек
с документом на 24 страницы, печатаемым с двух сторон, читал
«12 листов» и ждал 144 ₽ вместо 288 ₽. Она подтвердила и практикой:
«я за одну страницу брала».

Исправлено во всех местах разом: единица измерения, три ступени
(1–49 / 50–99 / от 100 страниц), сноска про двустороннюю печать,
витрина на главной, страница документов и описание страницы печати.

**Проверка текстов ещё поумнела.** Она читала HTML-комментарии как
текст для клиента и объявила выдуманной цену 8 ₽ — а та стояла
в комментарии, объясняющем, почему цифру поменяли. Теперь комментарии
выбрасываются до проверки. Правило то же, что и днём: проверять надо
то, что читает человек.

**Остановились на выкатке, и намеренно.** Файлы `9c5dcc9` забраны
на компьютер владелицы, но в Amvera НЕ загружены: в этот момент
на сайте человек оформлял заявку. Пересборка перезапускает приложение
на минуту-две, и нажатие «Отправить» в это окно заявку теряет —
она пишется в журнал уже на сервере. Её слова: «пока не буду».

**Следующий шаг для любого чата, который откроет эту беседу:**
в панели Amvera, приложение `granat-site`, вкладка «Репозиторий» —
загрузить из папки `C:\granat-kmv\zhivoj-sajt` файлы `app.py`,
`amvera.yaml` и папки `templates` и `static` целиком, затем «Собрать».
До этого момента на живом сайте старые цены копий и нет страниц
`/sertifikaty` и `/otkrytki`.

**Хвост, о котором её спросили и она не ответила:** добавлять ли
ДОКУМЕНТЫ в верхнее меню. Сейчас на `/dokumenty` попадают только
с карточки на странице печати и из поиска.

**И отдельно: цены бота остались старые.** `bot/prices.py` пересобран,
но он живёт в другом приложении Amvera — `jarvis-bot`, и туда его
никто не загружал. Пока не загрузим, Джарвис в Телеграме и ВК будет
называть копию по 10 ₽, распечатку по 12 ₽ и минимум 50 ₽ — то есть
спорить с сайтом.

**Шапка съехала — и это была моя правка.** Пятый пункт «Документы»
не поместился в строку, меню перенеслось на две и прижало логотип.
Владелица увидела сразу: «а почему тут смещение шапки сайта, всё
изменилось».

Починено без новых классов Tailwind — файл стилей собран заранее
(`npx tailwindcss ... --minify`), на её компьютере не пересобирается,
и класс, которого нет в собранном файле, просто не сработает.
Обошлись теми, что уже есть: `gap-6` вместо `gap-8`, `whitespace-nowrap`
на пункты, подпись «Печать, дизайн и полиграфия» сокращена
до «Печать и дизайн», кнопка — до «Узнать цену».

**Правило на будущее:** добавляя пункт в меню, сначала считать, влезет
ли строка. И не брать классы Tailwind, которых нет в собранном
`static/css/tailwind.css`.

**Заодно в микроразметке** сертификаты стояли по 300 ₽ — с тех же
времён, что и карточка на странице печати. Робот показывал в выдаче
цену, которой нет. Теперь 180 ₽, как на странице.

**«1 страниц — 1 листов» и обещание, которого не бывает.** Владелица
прогнала расчёт на одной странице и увидела две ошибки сразу.

Первая — падежи. Секретарь склеивал число со словом в одной форме:
«1 страниц — 1 листов». Это первое, что человек читает после суммы,
и небрежность здесь читается как небрежность в работе. Заведена
функция склонения, проверена на 1, 2, 5, 21 странице.

Вторая серьёзнее. На одной странице секретарь писал: «с двух сторон
вышло бы 1 листов по той же цене». Владелица ответила: «как это по той
же цене? 1 лист с двух сторон это уже 24 рубля» и следом «2 страницы
это один лист». Она права дважды: одна страница на две стороны
не ложится вовсе, а две страницы — это один лист и 24 ₽, потому что
считаем по страницам. Сравнение с двусторонней печатью теперь
показывается только от двух страниц.

**Фольгирование — не тиснение. Поправлено словами владелицы.** Она
сказала прямо: «на сайте только фольгой, а это называется
фольгирование. Тиснение фольгой на другой машинке делается, у меня
её нет».

До этого прайс называл услугу «Тиснение фольгой (фольгирование)»,
и это считалось находкой: клиенты ищут словом «тиснение». Но искать
словом и обещать услугу — разные вещи. Горячее тиснение делается
прессом с нагретым клише, а у нас фольга прикатывается ламинатором
на лазерную печать. Результат похож, технология другая, и человек,
приехавший за настоящим тиснением, уедет недовольным.

Переименовано везде: прайс, кнопка отделки у секретаря, строка счёта,
карточки на странице печати, страницы сертификатов и открыток,
описание сайта и микроразметка. «Тиснение золотом» на первом экране
стало «золотой фольгой». Горячее тиснение прессом добавлено в список
`NET_V_PRAJSE` — теперь секретарь и бот отвечают, что его нет.

**Хвост, ждём цифру владелицы.** У неё есть машинка «Остров сокровищ»
(серия ОС-5) — вырубка и сухое рельефное тиснение по шаблону. Услуга
живая, а цены на неё в прайсе НЕТ ни строки, значит сейчас она уходит
клиенту бесплатно. Предложено 200 ₽ за изделие: ручная отделка стоит
150 ₽, фольгирование 300 ₽, а тиснение прессом по трудозатратам ровно
между ними — приладка шаблона плюс прогон каждого листа руками.

**Не доделано в коде:**
- [ ] Завести «Тиснение и вырубка по шаблону» в прайс, когда владелица
      назовёт цену. Заодно убрать из `NET_V_PRAJSE` строку про слепое
      тиснение: машинка для него есть
- [ ] Скидка пенсионерам: решение владелицы 19.08.2026 — **со всей
      суммы заказа**, а не только с печати. Записать формулировкой
      в `SKIDKA_PENSIONERAM`, сейчас там просто «скидка 10%»

**Тиснение по шаблону заведено — 200 ₽ за штуку.** Цена владелицы,
её ответ одним словом на предложенную вилку. Машинка «Остров сокровищ»
ОС-5: рельеф и вырубка по шаблону, без фольги. До сегодняшнего дня
услуга делалась руками и в счёт не попадала — строки в прайсе не было.

Секретарь спрашивает про неё отдельной кнопкой рядом с фольгированием,
есть вариант «фольгирование и тиснение»: это разные машины и разные
работы, в счёте они складываются. Код отделки читается по кускам,
а не сравнением целиком, — иначе новое сочетание молча выпадет
из расчёта.

Из списка «чего нет» убрано слепое блинтовое тиснение: машинка для него
есть. Остались конгрев и УФ-лак.

**Проверка расчёта на живом заказе.** Грамота А4, матовая 230 г/м²,
макет с нуля, тиснение: 225 + 1000 + 200 = 1 425 ₽. Владелица
называла потолок: «я рассчитывала не больше 1500, если делать макет
такой грамоты». Прайс укладывается с запасом 75 ₽, менять ничего
не пришлось. Пенсионеру со всей суммы — 1 283 ₽. С готовым макетом
клиента — 425 ₽, пенсионеру 383 ₽.

Про сложные макеты прайс уже говорит верно: «дизайн с нуля — **от**
1 000 ₽», тысяча это нижняя граница.

**ВЫКАТКА `425b161` СДЕЛАНА, 19.08.2026 около 20:10.** Проверено живой
страницей `granat-kmv.ru/sertifikaty`: открывается, меню в одну строку
из пяти пунктов, шапка не съезжает, хлебные крошки на месте.

На живом сайте теперь всё сегодняшнее: копии и распечатка по страницам
12 / 10 / 9 ₽ без минимума, страницы `/sertifikaty` и `/otkrytki`,
пункт ДОКУМЕНТЫ в меню, фольгирование вместо «тиснения фольгой»,
тиснение по шаблону 200 ₽, склонения в расчёте копий.

**Что осталось несделанным на конец дня:**
- [ ] **Цены бота устарели.** `bot/prices.py` пересобран, но живёт
      в другом приложении Amvera — `jarvis-bot`, и туда не загружался.
      Джарвис в Телеграме и ВК называет копию по 10 ₽, распечатку
      по 12 ₽ и минимум 50 ₽ — то есть спорит с сайтом. Про тиснение
      и переименование фольгирования он тоже не знает
- [x] Три новые страницы отправлены на переобход в Яндекс Вебмастере
      19.08.2026 в 20:19, статус «В очереди»: `/dokumenty`,
      `/sertifikaty`, `/otkrytki`
- [ ] **`sitemap.xml` их всё равно не знает**: файл лежит в корне
      приложения, в репозитории его нет, править нечем. Переобход —
      это разовая мера, а карта сайта нужна постоянно. Забрать
      `sitemap.xml` из Amvera в репозиторий и добавить три адреса
- [ ] Скидка пенсионерам записана как «10%», а решение владелицы —
      **со всей суммы заказа**. Формулировку в `SKIDKA_PENSIONERAM`
      уточнить
- [ ] Замер расхода чернил 22.08: вся себестоимость стоит на оценках

### 19 августа, вечер — карточка в Яндекс Картах

Владелица: «реклама в поиске Карт показывает только номер телефона,
а адрес сайта не показывает. Сайт не посещают, не пользуются
калькулятором».

**Проверили карточку глазами клиента, и первая версия оказалась
неверной.** Я решил, что поле «Сайт» пустое. Открыли — сайт там есть,
`https://granat-kmv.ru/`, и кнопка в карточке тоже есть: маленькая
синяя иконка со стрелкой, без подписи, четвёртая в ряду после
«Маршрут», такси и телефона.

**Настоящая причина — зелёная кнопка «Посмотреть каталог»** во всю
ширину экрана. Её ставит Яндекс, потому что заполнены «Товары
и услуги», и ведёт она в витрину ВНУТРИ Карт, а не на сайт. Человек
жмёт заметное, а не безымянную иконку, и до калькулятора не доходит.

**Решение: не воевать с кнопкой, а завести каталог на сайт.** У каждого
товара в Яндекс Бизнесе есть поле «Ссылка на страницу товара». Ставим
туда наши страницы — `/sertifikaty`, `/otkrytki`, `/dokumenty`, — и
зелёная кнопка начинает работать на нас.

**Яндекс не принял ссылку, и первая догадка не подтвердилась.**
На «добавьте корректную ссылку» я решил, что дело в косой черте:
маршруты в `app.py` объявлены без неё, и Flask отдавал 404
на `/sertifikaty/`. Поставил `app.url_map.strict_slashes = False`.
Правка полезная сама по себе — люди дописывают слеш по привычке, —
но причина оказалась не в ней: Яндекс так же отверг и главную
страницу `https://granat-kmv.ru/`, которая открывается заведомо.

Похоже, поле ссылки просто недоступно без платного продвижения
товаров: над ним написано «товар будет кликабельным в Картах, если
активно продвижение», а вместо честного «недоступно» показывается
жалоба на адрес. Проверить, что даёт это продвижение и сколько
стоит, — до оплаты, а не после.

**Не доделано:**
- [ ] **Акция в карточке Карт не заведена в прайс**: «Скидка 10%
      на печать плакатов и чертежей». Висит у клиента на экране,
      а секретарь и бот называют полную цену. Нужны условия
      и срок от владелицы
- [ ] Цена в товаре «Грамоты сертификаты» проставлена 280 ₽ при
      прайсовых 180 ₽. Уточнить, что верно, и свести
- [ ] Ссылки на страницы проставить и в остальных товарах каталога

**Конец дня 19.08.2026.** Владелица ушла от компьютера: «я без
компьютера уже, поэтому чёрного окна сегодня уже не будет».

**НЕ ВЫКАЧЕНО: коммит `b2ab806`** — строка `strict_slashes = False`
в `app.py`. На живом сайте адрес с косой чертой на конце по-прежнему
даёт «страница не найдена». Выкатывать так же, как остальное: fetch,
checkout папки `zhivoj-sajt`, загрузка в `granat-site`, «Собрать».

**С чего начинать завтра, по порядку:**
1. Выкатить `b2ab806` — одна короткая правка, две минуты
2. Загрузить цены бота в `jarvis-bot`: он до сих пор называет копию
   по 10 ₽ и минимум 50 ₽, спорит с сайтом
3. Спросить условия акции «Скидка 10% на печать плакатов и чертежей»
   из карточки Карт и завести в прайс
4. Свести цену грамоты: в товаре Яндекс Бизнеса стоит 280 ₽, в прайсе
   180 ₽

### 20 августа — объявление про грамоты в Яндекс Бизнесе

Владелица разместила объявление в «Продвижении», срок **20.08 —
07.10.2026**. Текст закрывает две мысли клиента сразу, как она
и просила: «Разработаем макет — от 1000 ₽. Свой макет — печать
от 180 ₽/шт.» Ограничение поля — 81 знак, поэтому адрес убран:
он и так есть в карточке.

**Цены выправлены по прайсу перед публикацией.** В черновике стояло
«макет от 1500 ₽, печать от 150 ₽/шт». Обе цифры неверные:

- дизайн с нуля в прайсе — **от 1 000 ₽**;
- печать грамоты А4 — **от 180 ₽**, а 150 ₽ это розничный ПОРОГ
  на мелкий формат, не цена А4. Решение владелицы от 19.08: «вот
  смотри от 180 рублей, я думаю что это правильно».

Скидки в объявлении нет, цены совпадают с прайсом — значит секретарь
и бот скажут человеку то же, что он прочитал в Картах. Заводить
в `AKCII` нечего, и это правильный исход: объявление и прайс сошлись
до публикации, а не после жалобы клиента.

**Правило, подтверждённое на живом случае.** Цену в объявление берём
из прайса, а не из головы. Если хочется другую — сначала меняем прайс,
потом пишем объявление. Иначе они разъедутся за день, как уже
случилось с ценой сертификата: 300 ₽ на витрине против 180 ₽
в прайсе.

**Заказ на грамоту закрыт, цена подтвердилась живым клиентом.**
Клиентка забрала работу и осталась довольна, владелица попросила
её оставить отзыв в Яндексе.

Это первый заказ, посчитанный по новому: грамота А4 на матовой
фотобумаге 230 г/м² — 225 ₽, макет с нуля — 1 000 ₽, тиснение
по шаблону — 200 ₽, итого 1 425 ₽. Владелица называла потолок
«не больше 1500, если делать макет такой грамоты» — уложились
с запасом 75 ₽, и клиент цену принял.

Важное для прайса: **тиснение 200 ₽ прошло проверку продажей.**
До 19.08 эта работа делалась руками и в счёт не попадала вовсе.
